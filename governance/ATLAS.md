# Atlas

Single Source of Truth for Prometheus engineering sessions. Read this before development work.

## 1. Project Overview

Prometheus is a Python finance intelligence repository centered on the `knowledge/` package. It ingests company documents, creates company memory and business understanding artifacts, plans discovery questions, retrieves supporting chunks, and produces intelligence outputs.
It also defines a canonical Fundamentals Engine contract under `knowledge/financials` for financial statement normalization before downstream ratios, trends, CIM / PCIM, and investor analysis.

Atlas governs engineering work: architecture first, no duplicate modules, and governance updates when implementation changes architecture or module status.
The Prometheus Intelligence Manifesto in this folder is the governing product document for intelligence behavior, product explanation, and failure-learning standards.

## 2. Current Phase & Sprint

- Phase: Phase 15.3 — Formal Investor Certification V2 Baseline Execution (BLOCKED 2026-09-04 · `BLOCKED_CERTIFICATION_TARGET_SCOPE_UNFROZEN`).
- Sprint: Architecture Review Round 1.
- Rule: Documentation-only work unless a later sprint explicitly authorizes production code changes. The Ask IntrinsicIQ v0 frontend scaffold is an explicitly authorized production-code exception for `apps/ask-intrinsiciq/`.

**CERTIFICATION GOVERNANCE NOTE (recorded 2026-09-03):** The historical "28-question / 63/100 / B — PROMISING_BUT_NOT_INVESTOR_GRADE" certification baseline referenced in the Phase 15 mission document is `NON_REPRODUCIBLE`. It has never existed in any commit, stash, unreachable object, or governance artifact in this repository. This baseline MUST NOT be used as a formal quantitative benchmark. INVESTOR_CERTIFICATION_V2.0 is the new canonical certification standard — frozen 2026-09-03 in `governance/certification/investor_certification_v2.json` (SHA: `4769d7f3328e0a54973b2ce396ca5df23ada8bcd8574f1899e2926cb5e56ebb4`). Phase 15.3 (first baseline run) is next.

**V2 CERTIFICATION CANONICAL CONTRACT:**
- 25 questions frozen, 6 domains
- 3-tier verdict: ACCEPTED / PARTIAL / REJECTED
- NUMERIC_SCORING_ENABLED = NO (deliberate — avoids lost-formula failure)
- GRADE_TAXONOMY_ENABLED = YES: CERTIFIED / NEAR_CERTIFICATION / DEVELOPING / UNSAFE
- Investor-grade gate: ≥15 ACCEPTED + ≤2 REJECTED + 0 CFs + FI floor + MA floor
- Evaluator: deterministic Python only (no LLM)
- 66/66 calibration tests pass

### Phase 15.3 (Blocked)
Formal Investor Certification V2 Baseline Execution. Blocked at PART 2 (target scope verification). Contract hash verified (PASS: `4769d7f3...`). 68/68 integrity tests pass. Kill criterion triggered: frozen V2.0 contract contains no `production_target`, `company_selection_rule`, or `corpus_selection_rule` field. Without a frozen target, any baseline run would produce a company-specific result whose target was chosen post-freeze, violating the mission's reproducibility mandate. No production answers generated. No questions run. No code changes. Remediation: a new contract version must add a `production_target` field (with `company_slug`, `selection_rationale`, `corpus_scope`, `frozen_at`), recompute the hash, and run Phase 15.3 again. Status: `BLOCKED_CERTIFICATION_TARGET_SCOPE_UNFROZEN`. See ENG-085.


**BASELINE READINESS VALIDATION BLOCKER SIDEBAR (2026-09-05):** ENG-094 Ask next-question failure was schema/catalog drift: `answer_cards.py` owns the canonical curated Ask question catalog, and `validator.py` now derives valid question IDs from that catalog instead of a validator-local duplicate list. Production Ask validation now passes for Sun Pharma, Tanla, and Data Patterns. Management Quality dependency policy now treats Management Commitments and Management Progression as required validation streams, while Projects, Capacity, Risks, Management Commentary, and Capital Allocation Outcomes are optional enrichment streams; failed optional streams are quarantined/omitted and recorded in the MQ manifest rather than consumed. Required invalid streams still hard-fail. Tanla public-term failure was a false positive on substantive phrase "employee satisfaction score"; `score` is no longer a forbidden free-text substring, while score-like internal keys and `management_score` remain blocked. Strict baseline readiness remains blocked because Sun Pharma/Tanla MQ artifacts are warning-only, not PASS; safety review rejected suppressing optional-evidence warnings as validator weakening. See ENG-094 and ENG-101.

**ENG-104 SIDEBAR (2026-09-05 — Fisher/Graham hallucination elimination, COMPLETED):** Four structural affordances removed from the analyst prompt construction pipeline. Fix A: `_format_allowed_financial_metrics()` now uses `entry.get("canonical_metric")` (e.g. `"cfo"`) as `metric_name` value — was `entry.get("metric_id")` which returned the shortcode `"cfo:fy24"`. Fix B: `compact_financial_truth_for_analyst()` `source_provenance_summary` now always `[]` — was 4 filenames from `financial_truth_inputs.source_provenance`. Fix C: `company_memory_context.py` financial module summaries no longer include `"source_artifact": path.name`. Fix D: `source_artifact`, `source_artifacts`, `primary_artifact` added to `_INVESTOR_PANEL_DROP_FIELDS`; `source_artifact` removed from `_INVESTOR_PANEL_PREFERRED_KEYS`. Three deterministic tests added. Results: Fisher 36→0 ✓, Graham 41→4 (all ROUTING_REPAIR — `ev_fy26_company_intelligence_json_business_model_fy26` is valid in full PCIM but outside Graham's authorized section set), Buffett 0 ✓, Munger 0 ✓, Lynch 0 ✓. Semantic spot-check: canonical metric names in `financial_metrics_used`, no filenames in evidence citations. Remaining `evidence_grounding_status=warning`: from MISROUTED_VALID_EVIDENCE — outside ENG-104 scope. Cross-company regression: blocked by pre-existing PCIM validation failure on Tanla/DataPatterns. Baseline: `61_BASELINE_PROVENANCE_IDENTITY_CLEAN`. `PROVENANCE_HALLUCINATION_ELIMINATED_ENG_104`.

**ENG-103 SIDEBAR (2026-09-05 — metric_id structural fix, BLOCKED):** Fix implemented: `metric_id` → `metric_name` in `_format_allowed_financial_metrics()` formatted output (runner.py ~line 3572). Source data model unchanged. Two deterministic tests added and passing. Single production generation: Buffett 10→0 ✓, Munger 8→0 ✓, Lynch 11→0 ✓, Graham 26→41 ✗, Fisher 4→36 ✗. Total removed_invalid_ids: 77 (vs ~59). Key finding: Fisher still generates metric shortcodes (`cfo:fy24`, `gross_margin:fy26`) after the rename — LLM reproduces these from training distribution, not from prompt adjacency alone. Graham: section-name/filename class unchanged. All 5 analyst final artifacts: 0 invalid evidence IDs in saved output (sanitizer held). Cross-company regression: Tanla/DataPatterns unaffected. Provenance spot-check: all 5 analysts 100% valid IDs in final artifacts. Panel doctor: WARNING (unchanged). Baseline decision: `61_BASELINE_PARTIAL_METRIC_FIX_PANEL_WARNING`. Next: ENG-104 — investigate Fisher shortcode generation and Graham filename/section-name hallucination via separate structural approaches. `BLOCKED_ENG_103_FINANCIAL_METRIC_REFERENCE_FORMAT_CLEANUP`.

**ENG-102 SIDEBAR (2026-09-05 — investor panel provenance coherence repair, BLOCKED):** Three hallucination classes identified across the 5 investor panel analysts: (1) Graham: PCIM section names and `.json` artifact filenames; (2) Buffett/Fisher/Munger/Lynch: financial metric shortcodes (`gross_margin:fy26`, `cfo:fy24`) — LLM concatenates `metric_id` + `period` from the "Allowed Financial Metrics" prompt table into evidence IDs; (3) normalization-event dict warnings in `evidence_grounding_warnings` incorrectly degraded `evidence_grounding_status` from pass to warning. Fixes applied: prompt prohibition strengthened to explicitly name metric shortcodes; normalization-event dicts excluded from status degradation (runner.py ~line 7082); auto-carry confirmation string excluded from status degradation. Results: Graham 42→26, Fisher 25→4, Munger 17→8 removed IDs after first re-run. Stochastic regression on second re-run: Buffett 0→10, Lynch 0→11. Root cause of remaining failures: `_format_allowed_financial_metrics()` exposes `metric_id` and `period` as adjacent dict keys; the LLM forms `{metric_id}:{period}` references from them. Prompt prohibition insufficient — structural fix needed: rename `metric_id` → `metric_name` in the formatted output (ENG-103). Sanitizer correctly removes all invalid IDs from saved artifacts (artifact integrity invariant maintained). Committee synthesis/brief/QA: PASS. Panel doctor: WARNING. Baseline decision: `61_BASELINE_REQUIRES_REAUDIT` pending ENG-103. Sun Pharma Reality Audit baseline remains 61/100, Band B, `FIRST_TRUSTED_POST_CANONICAL_LIFECYCLE_BASELINE`. `BLOCKED_INVESTOR_PANEL_PROVENANCE_COHERENCE`.

**ENG-098A SIDEBAR (2026-09-05 — lifecycle downstream closure):** The ENG-098 closure note was too narrow: Ask and Management Quality did require code changes because active consumers could still read `management_commitments.status` / `delivery_assessment` as lifecycle truth. Fix: Ask Q-A/Q-B now declare and load `management_progression`, join MC records to MP only through `commitment_fingerprint`, and render missing MP matches as conservative Unable To Verify. Management Quality loads MP and derives commitment polarity from MP status; UTV/unknown is neutral/unclear, not positive delivery. Capital Allocation outcome evidence no longer prefers MC status/delivery-assessment notes. Gold remains MP-derived. Production convergence: Sun Pharma MC-0004, Sun Pharma MC-0013, and Tanla MC-0012 all resolve MC=UTV, MP=announced, Gold=CLAIM_ONLY/UNVERIFIED, Ask=UTV, MQ=neutral/unclear. Focused tests: 27 passed. `BLOCKED_LEGACY_LIFECYCLE_CONSUMER_MIGRATION` because strict downstream validation remains failing for unrelated MQ/Ask issues.

**ENG-098 SIDEBAR (2026-09-05 — does not change Phase 15.3 status):** Closure audit of ENG-097 (same session) found its governance claim `MANAGEMENT_COMMITMENT_LIFECYCLE_OWNERSHIP_REPAIR_CLOSED` was premature: verdict `ENG_097_PARTIAL_FIX_ONLY`. Two root defects remained: (1) the 0.20 overlap threshold remained an `UNSAFE_STATUS_GATE` — single domain words like "specialty" or "manufacturing" bridging unrelated items; (2) `MULTIPLE_COMPETING_LIFECYCLE_AUTHORITIES` persisted — MC, MP, and Gold each independently computed delivery status (MC-0013: MC=In Progress, MP=announced, Gold=UNVERIFIED; Tanla MC-0012: MC=Delivered, Gold=UNVERIFIED). Fix: canonical management lifecycle authority migration. Three files changed: (1) `knowledge/company_memory/management_commitments.py` — MC de-authorized as lifecycle authority: always outputs `status="Unable To Verify"` regardless of evidence content; `_determine_status()` retained internally as `_candidate_evidence_signal` only, never surfaced as top-level status; added `lifecycle_authority="management_progression"` semantic contract field; added stable `commitment_fingerprint` (sha256 of `company|period|normalized_commitment`, 20 hex chars) for cross-system linkage; removed `overstated_verification` validator (no longer valid); (2) `knowledge/management_progression/producer.py` — `_events_from_commitments()` removes all dependency on MC status; all commitment events set `verification_status="unresolved"` unconditionally; lifecycle advances only from action/milestone/completion/outcome events sourced from structured sources (projects, capacity, commentary, capital_allocation_outcomes); (3) `tests/knowledge/management_progression/test_lifecycle_authority_contract.py` — 17 new contract tests covering MC-status invariant, fingerprint stability, MP role-based classification, Gold derivation from MP, and canonical cases (MC-0004, MC-0013, Tanla MC-0012). Gold and Ask require no code changes: Gold already derived lifecycle from MP event roles; Ask's `_first_string()` short-circuits on MC's always-non-empty "Unable To Verify" string and cannot fall through to a stale status. Convergence proof: MC-0004 = UTV (MC) / announced (MP) / CLAIM_ONLY+UNVERIFIED (Gold) — consistent; MC-0013 = UTV (MC) / announced (MP) / UNVERIFIED (Gold) — consistent; Tanla MC-0012 = UTV (MC) / announced (MP) / UNVERIFIED (Gold) — consistent. Production: Sun Pharma 47 commitments all UTV, Tanla 28 all UTV, Data Patterns 28 all UTV. 126/126 prior tests pass + 17/17 new contract tests pass. Prior 56/100 baseline: `PRIOR_56_BASELINE_LIFECYCLE_CONTAMINATION: YES` — MC-0004 previously surfaced as Delivered; any audit reasoning relying on that state is invalidated. `CANONICAL_MANAGEMENT_LIFECYCLE_AUTHORITY_MIGRATION_CLOSED`. ENG-097 governance claim retracted and superseded. See ENG-098.

**ENG-097 SIDEBAR (2026-09-05 — does not change Phase 15.3 status; superseded by ENG-098):** `management_commitments.py` incorrectly classified Sun Pharma MC-0004 (Nafamostat/AQCH COVID-19 Phase-2 trials) as `Delivered` despite management_progression showing `UNABLE_TO_VERIFY` and Gold tracker showing `UNVERIFIED`. Root cause: a 3-level evidence contamination chain. (1) `_GENERIC_FALLBACK_TOPICS` did not include `"Product launch"` — the guard requiring text similarity never fired when two unrelated Product-category items shared that topic string. (2) The FY21 FluGuard item classified as `commitment_role="announcement"` (contains DELIVERED_MARKER "launched") and was compared via `_promises_match()` at 0.72, which it narrowly passed because its full text included "COVID-19" tokens. (3) For `follow_up`-role candidates, the guard checked only for any shared token — weak enough that "products"/"including"/"covid"/"19" bridged unrelated items at 0.167 overlap ratio. Fix: two changes to `knowledge/company_memory/management_commitments.py`: (1) expanded `_GENERIC_FALLBACK_TOPICS` to include all 9 `TOPIC_BY_CATEGORY` defaults ("Product launch", "Capacity expansion", "Technology upgrade", "Partnership rollout", "Financial target", "Margin improvement", "Market entry", "Commercial production", "Customer win") — these are category-level labels identical across unrelated initiatives and cannot serve as merge identity on their own; (2) raised follow_up overlap threshold from "any shared token" to `≥ 0.20` ratio — blocks FluGuard contamination (4/24=0.167 < 0.20) while passing a genuine Nafamostat follow-up (4/16=0.25 ≥ 0.20). Three-way conflict resolved: MC-0004 = Unable To Verify in all three artifacts. Cross-company regression: Tanla 28 commitments / 1 Delivered (MC-0012, pre-existing); Data Patterns 28 commitments / 0 Delivered. Sun Pharma: 47 commitments (23 → 47 because expanded guard correctly prevents over-merging distinct initiatives), 46 Unable To Verify, 1 In Progress (MC-0013, legitimate). `MANAGEMENT_COMMITMENT_LIFECYCLE_OWNERSHIP_REPAIR_CLOSED`. See ENG-097.

**ENG-086 SIDEBAR (2026-09-04 — does not change Phase 15.3 status):** `validate_committee_brief_source()` in `committee_brief_renderer.py` was failing on the Sun Pharma panel stage with `ValueError: committee_synthesis.json contains forbidden internal term: "evidence_id"`. Root cause: global substring scan on all string values in `evidence_quality_notes` matched `"evidence_id"` as a substring of `"unknown_evidence_id"` inside analyst exclusion message diagnostic strings. Fix: apply existing `_clean_phrase()` to each string value before the forbidden-term scan. Exclusion message diagnostics → rewritten to user-safe form → pass. True prose leakage → not rewritten → still blocked. The canonical Committee synthesis contract is unchanged. Structured `evidence_ids` keys (never collected by `_flatten_strings()`) are unaffected. 32 new provenance-compatibility tests. Sun Pharma, Tanla, Data Patterns panel stages all pass. `COMMITTEE_BRIEF_PROVENANCE_COMPATIBILITY_CLOSED`. See ENG-086 and ENG-087.

### Phase 15.2 (Closed)
Formal Investor Certification V2 Contract Definition + Immutable Freeze. Designed and froze INVESTOR_CERTIFICATION_V2.0. Key decisions: 25Q contract (all ALL_QUESTIONS), 3-tier verdict with hard/soft floor separation, NUMERIC_SCORING=NO, grade taxonomy enabled, 4 auto-detectable + 7 manual-audit critical failure classes, deterministic Python evaluator, evidence boundary locked to canonical loader + builder path. Harness updated with V2 verdict functions. 66 calibration tests pass (synthetic fixtures only, no production data accessed). SHA-256 recorded in contract. ENG-084 closed. Status: `CERTIFICATION_V2_CONTRACT_FROZEN`.

### Phase 15.1 (Closed)
Original Certification Contract Forensic Recovery. Phase 15 (Formal 28-Question Investor-Grade Re-Certification) blocked with `BLOCKED_ORIGINAL_CERTIFICATION_HARNESS_NOT_RECOVERABLE` because the current acceptance harness uses 25 questions and ACCEPTED/REJECTED only, with no PARTIAL verdict, no /100 scoring, and no grade taxonomy. Phase 15.1 performed exhaustive forensic recovery across all repository history. Scope: 8 commits, 1 pre-amendment commit (4e488f6), 1 dangling stash commit (71c080d), 2978 unreachable blobs, all stashes (0), full reflog, all branches (development + main), all governance documents at every commit version, all untracked working-tree files. Key finding: none of the required certification contract components — 28-question set, PARTIAL semantics, /100 scoring formula, Evidence Integrity scoring, Decision Usefulness scoring, grade taxonomy, investor-grade threshold, kill criteria, or original run artifacts — exist anywhere in repository history. The historical baseline existed only in the Phase 15 mission document (user-defined context), not in any machine-readable or governance artifact. Recovery confidence: NONE on all components. Formal verdict: `NON_REPRODUCIBLE_HISTORICAL_BASELINE`. Next mission: design and freeze Formal Investor Certification V2 (see ENG-084). Status: `ORIGINAL_CERTIFICATION_FORENSIC_RECOVERY_CLOSED`.

### Phase 14 (Closed)
Ask/UI Canonical Intelligence Synthesis Integration + Production Validation. Audited and repaired the final intelligence path: Canonical Company Intelligence + Investor Panel + Committee → Ask query synthesis → structured answer → UI output. Three bottlenecks found: (1) Committee `doctrine_disagreements` silently dropped — `_sentence_truncate()` produced `;`-terminated text that `_is_obviously_truncated_text()` rejected; fix converts `;` endings to `.` in both committee builders; (2) `management_credibility_signal` never reached user-facing answers — added to `summarize_progression_item()` "claim" kind return dict and wired into `_progression_key_points()` for `did-past-claims-come-true`; (3) `longitudinal_current_state` unused at Ask layer — added `_augment_with_lcs_signals()` helper (respects 4-item cap) and wired into `_build_canonical_progression_answer()` and `_build_past_claims_answer()`. 20 focused tests in `tests/intelligence/test_phase14_ask_canonical_synthesis.py`; all 20 pass, same 16 pre-existing baseline failures unchanged. Status: `ASK_CANONICAL_SYNTHESIS_INTEGRATION_CLOSED`.

### Phase 13 (Closed)
Investor Panel + Committee Canonical Intelligence Integration Audit & Targeted Repair. Audited whether the five Panel specialists (Graham, Buffett, Fisher, Munger, Lynch) and Committee Synthesizer consume canonical upstream intelligence (Company Model LCS, Management Progression lifecycle fields, PCIM). Three bottlenecks found and repaired in `intelligence/investor_panel/company_memory_context.py`: (1) `longitudinal_current_state` was invisible to Panel — added `_compact_longitudinal_current_state()` and wired into `_compact_company_model()`; (2) MP items had no lifecycle metadata and limit was 2 — added `stream_types`, `management_credibility_signal`, `current_status` and raised limit to 4; (3) Munger's `DOCTRINE_MEMORY_PRIORITIES` listed `company model` at position #10 (never reached LLM) — moved to position #4. Verification: Tanla all 5 specialists get `lcs=True, mp_items=4, mp_has_credibility=True`; Data Patterns 4/5 specialists get `lcs=True` (Munger correctly gets management_quality). 25 focused tests written; 985 passed, 0 regressions. Status: `INVESTOR_PANEL_COMMITTEE_INTEGRATION_CLOSED`.

### Phase 12.1 (Closed)
Company Model Longitudinal Propagation Repair. Extended the Phase 12 longitudinal chain into Company Model. Before this phase, Company Model read only from PCIM/CIM (annual-report-only, legacy adapter) and had no access to multi-source longitudinal intelligence. Repair: `knowledge/company_model/evidence_adapter.py` — added `"management_progression"` to `GOVERNED_SOURCE_FILES` (not LEGACY_ADAPTER_SOURCE_NAMES). `knowledge/company_model/producer.py` — added `_build_longitudinal_current_state()` reading MP items with `stream_types=["multi_source_longitudinal"]`, producing compact state entries (max 12, no event history). `knowledge/company_model/validator.py` — added `_validate_longitudinal_current_state()` enforcing ownership boundary (events key forbidden, raw text forbidden). Production proof: Tanla validation=pass 10 LCS items; Data Patterns validation=pass 5 LCS items. 15 focused tests + 28/28 Company Model tests passing. 903/903 knowledge tests passing. Architecture verdict: `CURRENT_STATE_CANONICAL`. Status: `COMPANY_MODEL_LONGITUDINAL_PROPAGATION_CLOSED`.

### Phase 12 (Closed)
Multi-Source Longitudinal Wiring to Management Progression. Audit confirmed `longitudinal_report.json` had zero downstream consumers — 341 atoms/17 threads for Tanla isolated from all intelligence artifacts. Repair 1: `knowledge/management_progression/evidence_adapter.py` added `"multi_source_longitudinal": "longitudinal/longitudinal_report.json"` to DEDICATED_SOURCES. `knowledge/management_progression/producer.py` added `_events_from_multi_source_longitudinal()`. Production proof: Tanla 10/25 MP items longitudinal-sourced; Data Patterns 5/25. Repair 2: PCIM already read management_progression.json via `pcim_multi_year_builder.py` (pre-existing). Chain established: longitudinal → MP → PCIM → Company Model (Phase 12.1). Status: `LONGITUDINAL_TO_MP_CHAIN_CLOSED`.

### Phase 11.3 (Closed)
Platform-Launch Theme Precision Cleanup: ENG-078. Fixed `platform_launch` theme false positives caused by overly generic optional keywords `"new"` and `"announce"`. Root cause: `"new"` fires on "new customers", "new hires", "new accounting standards" when co-occurring with "platform" in the same chunk. Fix: removed `"new"` and `"announce"` from optional, keeping only `["launch", "gigantic"]`. A strong action verb is now required for platform_launch classification. Zero company-specific code. Data Patterns: 3 → 0 false atoms. Tanla: 21 → 3 atoms (18 false positives eliminated, 2 TRUE_POSITIVEs + 1 AMBIGUOUS preserved). Longitudinal rebuild clean: 17 Tanla / 7 Data Patterns threads, no stop conditions, no platform_launch thread in Data Patterns. 53 tests added in `tests/multi_source/test_eng078_platform_launch.py`. 118/118 multi_source tests passing. Architecture verdict: `SAFE_WITH_CURRENT_THEME_MATCHER`. ENG-078 CLOSED. ENG-079 open (monitor `"international"` optional in international_expansion — marginally generic, no current false positives). Status: `PLATFORM_LAUNCH_THEME_PRECISION_CLOSED`.

### Phase 11.2 (Closed)
Theme Registry Generalization Cleanup: ENG-077. Fixed cross-company theme contamination in `intelligence/multi_source/theme_registry.py`. Root cause: short pure-alpha abbreviations (≤ 4 chars) matched via plain substring, causing false positives on embedded occurrences ("bottom" → `ott`, "allotted" → `ott`, "patterns" → `pat`). Fix: generic `_kw_match()` function with word-boundary regex for all pure-alpha keywords ≤ 4 chars. Zero company-specific code. Zero theme-entry changes. Data Patterns: 5 → 0 `ott_whatsapp_growth` false atoms. Tanla: 24 valid OTT atoms preserved (1 `bottleneck` false positive removed). Longitudinal rebuild clean: Tanla 17 threads / Data Patterns 8 threads, no stop conditions, no OTT thread in Data Patterns. 41 tests added in `tests/multi_source/test_eng077_theme_registry.py`. Architecture verdict: `SAFE_WITH_CURRENT_REGISTRY`. ENG-077 CLOSED. Status: `THEME_GENERALIZATION_PRECISION_CLOSED`.

### Phase 11.1 (Closed)
Multi-Source Integration Contract Cleanup: ENG-059 + ENG-076. Fixed two upstream defects before downstream intelligence propagation. **ENG-059**: generic `company_names.json` reader added to `_build_company_registry()` in `knowledge/document_identifier.py`; `companies/datapatterns/company_names.json` populated. Data Patterns resolution: 3/15 → 14/15 documents HIGH confidence. **ENG-076**: `_parse_participant_list()` extended to handle BSE-style "MANAGEMENT: MR. NAME" headers and bare "MODERATOR:" terminators; added `_RE_BARE_SPEAKER_LABEL` and `_RE_PARTICIPANT_HONORIFIC` constants. Data Patterns Q1 FY27 transcript: 0 → 536 claims, 0 → 47 evidence atoms. Tanla regression: 341 atoms (unchanged), 67 transcript, 0 contamination. 20 new tests added (8 ENG-059, 12 ENG-076). Suite: 1120 passed. Status: `MULTI_SOURCE_INTEGRATION_CONTRACTS_CLOSED`. ENG-059 CLOSED. ENG-076 CLOSED.

### Phase 11 (Closed)
Data Patterns Multi-Source Generalization + Investor Intelligence Quality Evaluation. 10 Data Patterns non-annual documents processed through canonical workflow (operator bootstrap, all 6 source processors). Evidence corpus: 1→4 source families, 52→82 atoms, 5→8 themes. BEFORE/AFTER 12-question investor intelligence evaluation (7-dimension rubric, same as Tanla): BEFORE avg 16.3/35, AFTER avg 22.8/35, +40% lift (12/12 questions improved, 0 degraded). Largest gains: STAC acquisition (+12), Q1 FY27 performance (+11), capital allocation (+10), order wins (+9). Generalization verdict: STRONG — all 6 processors and multi-source aggregator generalize without company-specific code changes. Key bottlenecks documented: identifier company-resolution failure (ENG-059 open), transcript↔aggregator file mismatch (ENG-076 new). Status: `DATA_PATTERNS_MULTI_SOURCE_GENERALIZATION_CLOSED`.

### Phase 10.3 (Closed)
ENG-074 Legacy Path Cleanup + Canonical Inbox Finalization. All active runtime `data/annual_reports/` fallbacks removed from `run_company_pipeline.py`, `run_business_pipeline.py`, `knowledge/business_understanding/pipeline.py`, `scripts/verify_clean_chunks.py`, `embeddings/index_builder.py`, `scripts/run_pdf_pipeline.py`. All test fixtures updated to Processed/ paths. The two content-duplicate PDFs remaining in `data/annual_reports/` (SHA-256 verified against primaries) were deleted and the directory removed. A regression guard (`tests/knowledge/test_no_legacy_annual_reports_path.py`) now fails the suite if runtime code reintroduces the retired path. All 7 production smoke proofs pass. 876 tests passing. Status: `CANONICAL_INBOX_WORKFLOW_CLOSED`. ENG-074 CLOSED.

### Phase 10.2 (Closed)
Execute Inbox Migration + Production Workflow Validation. Migrated all 61 PDFs from `data/annual_reports/` into canonical workflow: 51 → `data/Processed/`, 7 → `data/Review/`, 1 → `data/Failed/`, 2 content-duplicates skipped. Registry: 59 entries, 59/59 SHA-256 verified. Fixed dry-run registry contamination bug (all registry writes now gated on `execute` flag). 869 tests passing. Status: `INBOX_MIGRATION_PRODUCTION_VALIDATED`. ENG-073 CLOSED.

### Phase 10.1 (Closed)
Canonical Document Inbox + Processed Archive + Global Idempotent Processing Registry. `core/inbox_paths.py` is single canonical path owner. `core/document_intake_registry.py` is SHA-256 keyed global registry (atomic writes, semantic fingerprint, 5-outcome `decide_processing_action()`). `pipelines/scan_inbox.py` is crash-safe scanner. `pipelines/migrate_annual_reports.py` is the historical migration utility (completed; legacy-only). 29 tests passing. Status: `DOCUMENT_INBOX_IDEMPOTENCY_CLOSED`.

### Phase 10 (Closed)
Tanla multi-source expansion + BEFORE/AFTER intelligence quality evaluation. 13 new documents processed across EARNINGS_RELEASE and QUARTERLY_REPORT families. `QUARTERLY_REPORT` added to `SourceAuthority` and all 5 authority rank tables. New loaders in `aggregator.py`. Source coverage: 6 source families, 341 evidence atoms (up from 222), 17 commitment threads. BEFORE avg: 21.4/35 → AFTER avg: 24.0/35 (+12% lift across 12 investor questions). Primary gains: temporal depth (+1.25/5) and source breadth (+1.00/5). Status: `TANLA_MULTI_SOURCE_QUALITY_EVALUATION_CLOSED`. ENG-067 CLOSED.

### Phase 9 (Closed)
Multi-source longitudinal intelligence integration. `intelligence/multi_source/` builds the canonical said→did→outcome path from all 6 processor output families. Tanla production proof: 4 source families, 17 commitment threads, `leadership_change` CONFIRMED by EXCHANGE_DISCLOSURE. Status: `MULTI_SOURCE_LONGITUDINAL_INTEGRATION_CLOSED`. ENG-066 CLOSED.

## 3. Canonical Architecture

Canonical architecture, summarized:

1. Company/year context is created.
2. Raw documents are read and chunked by `scripts/smart_chunker.py`.
3. Company memory is built.
4. Business blueprint is interpreted.
5. Business classification is produced.
6. Discovery questions are planned.
7. `knowledge/retrieval` provides supporting chunks.
8. Discovery runtime executes module extraction.
9. CIM / PCIM intelligence artifacts are written.
10. `multi_year_memory` derives order-independent company-level history from yearly intelligence snapshots. A new deterministic company-memory commitments layer, `management_commitments`, now reads explicit yearly management promise evidence and writes `companies/<company>/company_memory/management_commitments/management_commitments.json`, `commitment_timeline.json`, `commitment_validation.json`, and `management_commitments_manifest.json`. It normalizes commitments across time, records announcement-to-evidence progression, and refuses unsupported delivery claims without introducing any new LLM call.
11. `intelligence/progression` is the shared progression kernel for evidence-first change tracking. It owns the canonical contracts (`ProgressionEvent`, `ProgressionState`, `ProgressionTimeline`, `TurningPoint`, `ProgressionSummary`), generic confidence and validation helpers, deterministic deduplication, and manifest metadata for progression streams. Domain interpretation remains outside the shared engine.
12. `company_memory/management_commitments` is the first consumer of `intelligence/progression`. It maps explicit management promises into progression events and state transitions, but keeps commitment-specific judgment, validation, and status semantics inside the management-commitments module.
13. `company_memory/projects` is the second consumer of `intelligence/progression`. It interprets bounded project evidence as execution progress, keeps economic impact separate from completion, and writes `companies/<company>/company_memory/projects/projects_registry.json`, `project_timelines.json`, `project_assessments.json`, `projects_validation.json`, and `projects_manifest.json` without introducing a new LLM call.
14. `company_memory/capacity` is the third consumer of `intelligence/progression`. It normalizes capacity-evolution evidence into canonical company memory, keeps funding deployment separate from operating utilization, and writes `companies/<company>/company_memory/capacity/capacity_registry.json`, `capacity_timelines.json`, `capacity_assessments.json`, `capacity_validation.json`, and `capacity_manifest.json` without introducing a new LLM call.
15. `company_memory/risks` is the fourth consumer of `intelligence/progression`. It normalizes business and financial risk evidence into canonical company memory, tracks risk progression and materiality, and writes `companies/<company>/company_memory/risks/risk_registry.json`, `risk_timelines.json`, `risk_assessments.json`, `risk_validation.json`, and `risk_manifest.json` without introducing a new LLM call.
16. `company_memory/management_commentary` is the fifth consumer of `intelligence/progression`. It normalizes recurring management narrative into canonical company memory, tracks how emphasis, confidence, and conviction evolve across years, and writes `companies/<company>/company_memory/management_commentary/commentary_themes.json`, `commentary_timelines.json`, `commentary_assessments.json`, `commentary_validation.json`, and `commentary_manifest.json` without introducing a new LLM call.
17. `company_memory/capital_allocation_outcomes` is the sixth consumer of `intelligence/progression`. It normalizes capital deployment outcomes into canonical company memory, tracks how capital deployment, follow-through, and later payoff evolve across years, and writes `companies/<company>/company_memory/capital_allocation_outcomes/capital_allocation_outcomes.json`, `capital_allocation_timelines.json`, `capital_allocation_assessments.json`, `capital_allocation_validation.json`, and `capital_allocation_manifest.json` without introducing a new LLM call.
18. `company_memory/management_quality` is the seventh consumer of `intelligence/progression` in spirit, but it is a synthesis layer rather than a progression adapter. It reads the longitudinal memory already produced by commitments, projects, capacity, risks, management commentary, capital-allocation outcomes, owner earnings, per-share compounding, financial truth, and management-related investor-panel evidence, then writes `companies/<company>/company_memory/management_quality/management_quality_summary.json`, `management_quality_dimensions.json`, `management_quality_evidence.json`, `management_quality_validation.json`, and `management_quality_manifest.json` without introducing a new LLM call or collapsing the result into a single opaque score.
19. Shared company-memory guardrails now sit in `knowledge/company_memory/guardrails.py`. Before a candidate can influence progression or synthesis, it must clear business-relevance classification, period resolution, and progression-materiality checks; items that are historical context, outside the analysis window, ambiguous, or semantically non-core are quarantined instead of being promoted into canonical memory.
19. `intelligence/investor_panel/company_memory_context.py` is the investor-panel longitudinal context adapter. It converts company-memory progression streams into compact doctrine-prioritized summaries so the analyst prompt can read change over time without dumping raw company-memory artifacts or turning the panel into a screener.
20. `apps/ask-intrinsiciq/src/lib/prometheus/ask-v2/` is the canonical Ask IntrinsicIQ v2 answer-routing layer. It routes questions by intent, prefers governed company-memory evidence before raw-document fallback, preserves the public answer contract (`Question -> Conclusion -> Progression -> Why it matters -> Evidence -> Raw numbers`), and keeps backward-compatible UI payloads without adding new LLM calls or company-specific hardcoding.
17. `knowledge/failure_learning` is the canonical dated incident registry for production failures. It separates observed facts from root-cause hypotheses, assigns stable category paths, and records durable safeguards plus regression requirements for later learning and review.
18. `knowledge/archetypes` provides the curated deterministic registry and pack vocabulary used to normalize multi-year themes, risks, metrics, and capital-allocation signals.
17. The Fundamentals Engine canonical path starts with aggregate year-level `financials`, which orchestrates `financial_discovery` -> `financial_extraction` -> `financial_normalization` -> `financial_validation` -> `financial_reconciliation` -> `financial_ratios` -> `financial_growth` -> `corporate_actions` -> `shareholding_pattern`, then writes `financial_audit_report.json`. A separate deterministic year-level `financial_basis_resolution` stage now resolves standalone/consolidated basis from generic document, discovery, and table evidence, writes `financial_basis_resolution.json`, and may safely enrich yearly normalized fundamentals plus existing truth-registry artifacts only when confidence is medium/high. A separate deterministic year-level `financial_truth_registry` stage now reads existing yearly financial artifacts and writes `financial_fact_registry.json`, `financial_truth_reconciliation_report.json`, and `financial_artifact_quarantine_report.json`, which together form the canonical financial fact registry / truth-reconciliation / invalid-artifact quarantine foundation for contradiction detection and downstream financial-readiness judgment. The `financial_quality` stage is dual-mode: at year scope it reads yearly financial artifacts and writes `companies/<company>/<year>/financials/financial_quality_summary.json`; at company-memory scope, `financial_memory` orchestrates `financial_trends` -> `financial_quality` -> `financial_attribution`, then deterministically derives the reusable multi-year financial memory set (`financial_year_index.json`, `financial_memory_manifest.json`, `financial_truth_pack.json`, `financial_trends.json`, `financial_quality_evolution.json`, `capital_allocation_financial_timeline.json`, `ownership_evolution.json`, `financial_memory_summary.json`, `financial_quality_summary.json`, `financial_driver_attribution.json`), and finally writes `financial_memory_audit_report.json`. Company-memory financial artifacts must now prioritize the year-level truth engine (`financial_fact_registry.json`, `financial_truth_reconciliation_report.json`, `financial_basis_resolution.json`, `financial_artifact_quarantine_report.json`) over degraded summary regeneration. A separate deterministic company-level `investor_financials` stage now builds investor-facing financial intelligence modules on top of the reconciled company-memory truth pack and writes `owner_earnings_bridge.json`, `capital_allocation_roi_ledger.json`, `working_capital_quality_drilldown.json`, `order_revenue_cash_conversion_tracker.json`, `per_share_compounding_analysis.json`, and `investor_financial_modules_manifest.json` under `companies/<company>/company_memory/financials/investor_financial_modules/`. These modules must remain deterministic, generic, and truth-pack-backed; they may not read stale broad warning text as if it were primary evidence and may not call an LLM. A separate deterministic `financial_pcim_validation` stage now validates year-specific financial PCIM safety against those audited artifacts, and company-level `audit` also writes a `financial_quality_scorecard` before Investor Panel reasoning is treated as financially safe.
16. Committee synthesis now combines the five analyst outputs into a canonical `committee_synthesis.json` through a deterministic skeleton-first architecture. Committee synthesis v2 adds a deterministic evidence-and-progression overlay that preserves committee brief compatibility while surfacing shared convictions, disagreements, turning points, judgment blocks, and evidence confidence without adding a new LLM call. The pipeline owns metadata, analyst coverage, canonical enums, financial warning manifests, and registry-grounded `critical_unknowns`, while the LLM is limited to narrative fill such as executive summary and watch-list prose. The committee brief / QA stages then render and verify that combined story plus financial judgment without reading raw documents directly. The v2 contract is documented in `docs/architecture/COMMITTEE_SYNTHESIS_V2.md`.
17. Fundamentals acceptance QA now sits above the audited fundamentals path and writes a final company-level readiness judgment under `companies/<company>/audit/` before the fundamentals engine is treated as trusted for investor analysis.
18. Ask IntrinsicIQ now also has a deterministic company-level backend output layer under `intelligence/ask_intrinsiciq/`. Its explicit `ask_intrinsiciq` stage reads existing company-memory sources when available, introduces no new LLM call, and writes only under `companies/<company>/company_memory/ask_intrinsiciq/`.
21. `multi_year/risk_evolution.json` is the canonical identity owner for company risks. Its top-level `worsening_risks`, `improving_risks`, and `recurring_risks` lists hold semantic risk IDs (e.g., `risk_foreign_exchange_risk`) that are the ground truth for longitudinal trajectory. These IDs propagate through the risk pipeline via the `evo_canonical_id` field on `RiskDefinition` and `RiskAssessment`; trajectory signals propagate via `evo_trajectory`. The join is deterministic set-membership — no fuzzy matching, no string distance, no embeddings. Validator rules 26–30 enforce trajectory truth in all risk artifacts. `conviction_impact` is never inferred from trajectory alone.

Artifact roles:

- Cleaned project status: when a valid sourced project has no explicit lifecycle status, the project cleaner must preserve it with `status = "UNKNOWN"` and an uncertainty reason. It must not infer planned, ongoing, completed, or commissioned status from mere mention of a facility or capability.

- CIM / PCIM: canonical intelligence contract.
- `company_memory`: transient runtime artifact.
- `company_memory/management_commitments`: deterministic history layer for management commitments, promises, updates, delivery judgments, and manifest-tracked source coverage across time. It is canonical company-memory intelligence, not a scorecard.
- `company_memory/projects`: deterministic history layer for bounded projects, progression, execution status, impact separation, and manifest-tracked source coverage across time. It is canonical company-memory intelligence, not a dashboard.
- `company_memory/capacity`: deterministic history layer for capacity evolution, utilization evidence, funding-vs-operations separation, and manifest-tracked source coverage across time. It is canonical company-memory intelligence, not a performance scoreboard.
- `company_memory/risks`: deterministic history layer for business and financial risk evolution, progression tracking, materiality assessment, and manifest-tracked source coverage across time. Canonical risk identity is owned by `multi_year/risk_evolution.json`; trajectory (worsening/improving/recurring) propagates via `evo_trajectory` and `evo_canonical_id` fields through `RiskDefinition` and `RiskAssessment`. It is canonical company-memory intelligence, not a risk scorecard.
- `company_memory/management_commentary`: deterministic history layer for recurring management narrative, emphasis shifts, conviction changes, and manifest-tracked source coverage across time. It is canonical company-memory intelligence, not a scorecard.
- `company_memory/capital_allocation_outcomes`: deterministic history layer for capital deployment outcomes, later payoff, cross-stream links, and manifest-tracked source coverage across time. It is canonical company-memory intelligence, not a scorecard.
- `company_memory/management_quality`: deterministic synthesis layer for longitudinal management quality judgment across commitments, execution, capacity, risks, candor, strategy, capital allocation, owner alignment, and evidence confidence. It is canonical company-memory intelligence, not a scorecard.
- `intelligence/progression`: shared progression kernel for evidence-first change tracking. It standardizes event ordering, state transitions, confidence, turning points, and validation for multiple streams while leaving domain judgment outside the engine.
- `business_blueprint`: reasoning artifact.
- `business_classification`: classification artifact.
- `knowledge/archetypes`: curated archetype-pack registry activated by Business DNA for deterministic normalization.
- `knowledge/financials`: canonical fundamentals schema, unit-normalization, validation, corporate-action, and ownership-tracking contract.
- Financial monetary normalization: canonical normalized unit is INR crore (`value_crore`), while original reported value/unit must always be preserved.
- Financial schema rule: no LLM-calculated ratios or derived math belongs in the schema layer.
- Financial-memory eligibility is company-generic and year-complete: before building `financial_year_index.json`, the financial-memory stage must deterministically refresh `financial_quality_summary.json` for every discovered fiscal-year directory that has normalized fundamentals, then build the separate company-memory quality summary. A missing historical year-level quality artifact must not silently reduce a multi-year company to one usable year when the underlying validated financial artifacts are present.
- Investor-panel freshness is fail-closed: saved PCIM must not be used when governed `financial_truth_pack.json` or `investor_financial_modules_manifest.json` is newer than `pcim_v1.json`; CIM/PCIM must be rebuilt first. Analyst financial prompt compaction must order dated financial buckets and current metrics latest-year-first, while retaining older periods as trend context, and series-valued metrics must resolve their latest point by fiscal-year value rather than source-list position.
- Corporate-action rule: share-count-sensitive events must be captured as deterministic adjustment metadata, and historical EPS / per-share growth must not be silently restated in this layer.

- Promise tracking ownership: Management Commitments is the canonical source of promise identity and defines the trackable commitment universe through stable `commitment_fingerprint` values. Management Progression owns lifecycle truth only when it carries the exact canonical commitment fingerprint. Gold Promise Tracker must start from material Management Commitments, enrich only from exact-fingerprint Management Progression rows, preserve unmatched commitments as unverified, and never promote arbitrary progression/project/capital-allocation/risk rows into promises. Ask displays Gold promise records/counts and must not create a separate promise universe.

## 4. Canonical Pipelines

- Production pipeline: `pipelines/run_company_pipeline.py`.
- `pipelines/run_company_pipeline.py` now exposes two governed top-level orchestration profiles: `--stage all`, which remains the backward-compatible core-intelligence sequence (`preflight -> discovery -> extraction -> cleaning -> business_understanding -> business_intelligence -> intelligence -> multi_year_memory -> cim -> pcim`), and `--stage production`, which is the explicit customer-output sequence (`preflight -> discovery -> extraction -> cleaning -> business_understanding -> business_intelligence -> intelligence -> financials -> multi_year_memory -> financial_memory -> investor_financials -> cim -> pcim -> financial_pcim_validation -> audit -> panel -> ask_intrinsiciq`). Financial CIM/PCIM finalization must occur after financial memory and investor-financial modules so the investor panel cannot inherit a pre-financial snapshot. Both profiles must execute through the shared `run_stage_sequence(...)` helper rather than duplicated inline orchestration logic, and both must emit a `run_summary.json` with completed/failed/skipped stage reporting plus total runtime.
- `pipelines/run_company_pipeline.py` now also exposes the company-level `management_commitments` stage. Run it as `python pipelines/run_company_pipeline.py <company> --stage management_commitments` to generate the canonical management-commitments memory bundle under `companies/<company>/company_memory/management_commitments/`. It stays outside the default `all` chain because it is a deterministic company-memory intelligence layer, not a prerequisite raw-intelligence stage.
- `pipelines/run_company_pipeline.py` now also exposes the company-level `management_commentary` stage. Run it as `python pipelines/run_company_pipeline.py <company> --stage management_commentary` to generate the canonical management-commentary memory bundle under `companies/<company>/company_memory/management_commentary/`. It stays outside the default `all` and `production` chains because it is a deterministic company-memory intelligence layer, not a prerequisite raw-intelligence stage.
- `pipelines/run_company_pipeline.py` now also exposes the company-level `capital_allocation_outcomes` stage. Run it as `python pipelines/run_company_pipeline.py <company> --stage capital_allocation_outcomes` to generate the canonical capital-allocation outcomes memory bundle under `companies/<company>/company_memory/capital_allocation_outcomes/`. It stays outside the default `all` and `production` chains because it is a deterministic company-memory intelligence layer, not a prerequisite raw-intelligence stage.
- `pipelines/run_company_pipeline.py` now also exposes the company-level `management_quality` stage. Run it as `python pipelines/run_company_pipeline.py <company> --stage management_quality` to generate the canonical management-quality synthesis bundle under `companies/<company>/company_memory/management_quality/`. It stays outside the default `all` and `production` chains because it is a deterministic company-memory synthesis layer, not a prerequisite raw-intelligence stage.
- `pipelines/run_company_pipeline.py` now also exposes the company-level `projects` stage. Run it as `python pipelines/run_company_pipeline.py <company> --stage projects` to generate the canonical projects memory bundle under `companies/<company>/company_memory/projects/`. It stays outside the default `all` and `production` chains because it is a deterministic company-memory intelligence layer, not a prerequisite raw-intelligence stage.
- `pipelines/run_company_pipeline.py` now also exposes the company-level `capacity_evolution` stage. Run it as `python pipelines/run_company_pipeline.py <company> --stage capacity_evolution` to generate the canonical capacity memory bundle under `companies/<company>/company_memory/capacity/`. It stays outside the default `all` and `production` chains because it is a deterministic company-memory intelligence layer, not a prerequisite raw-intelligence stage.
- `pipelines/run_company_pipeline.py` now also exposes the company-level `risk_evolution` stage. Run it as `python pipelines/run_company_pipeline.py <company> --stage risk_evolution` to generate the canonical risk memory bundle under `companies/<company>/company_memory/risks/`. It stays outside the default `all` and `production` chains because it is a deterministic company-memory intelligence layer, not a prerequisite raw-intelligence stage.
- Future progression consumers should use the shared `intelligence/progression` package and keep stream-specific interpretation inside their adapters. The first six consumers are management commitments, projects, capacity, risks, management commentary, and capital allocation outcomes; `management_quality` is a synthesis layer that sits above them. Likely follow-ons are initiatives and other adjacent memory layers.
- Business-understanding stage: `knowledge/business_understanding/pipeline.py`.
- Multi-year company-memory stage: `multi_year_memory` via `knowledge/company_memory/multi_year.py`.
- Fundamentals Engine stages currently include aggregate year-level `financials`, which runs `financial_discovery`, `financial_extraction`, `financial_normalization`, `financial_validation`, `financial_reconciliation`, `financial_ratios`, `financial_growth`, `corporate_actions`, and `shareholding_pattern`, then writes `companies/<company>/<year>/financials/financial_audit_report.json`; the year-level `financial_basis_resolution` stage, which reads deterministic yearly financial artifacts and writes `companies/<company>/<year>/financials/financial_basis_resolution.json`; the year-level `financial_truth_registry` stage, which reads deterministic yearly financial artifacts including `raw_financial_tables.json` when present and writes `companies/<company>/<year>/financials/financial_fact_registry.json`, `financial_truth_reconciliation_report.json`, and `financial_artifact_quarantine_report.json`; the year-level `financial_quality` stage, which reads yearly financial artifacts and writes `companies/<company>/<year>/financials/financial_quality_summary.json`; the year-level `financial_pcim_validation` stage, which validates `company_memory/pcim_v1.json` against the active year’s deterministic financial artifacts and writes `companies/<company>/<year>/financials/financial_pcim_validation.json`; the company-level `investor_financials` stage, which reads reconciled company-memory financial truth and writes deterministic investor modules under `companies/<company>/company_memory/financials/investor_financial_modules/`; plus aggregate company-level `financial_memory`, which runs `financial_trends`, `financial_quality`, and `financial_attribution`, derives the multi-year financial memory artifacts, and then writes `companies/<company>/company_memory/financials/financial_memory_audit_report.json`.
- `ask_intrinsiciq` is now a canonical company-level stage on `pipelines/run_company_pipeline.py`. Run it as `python pipelines/run_company_pipeline.py <company> --stage ask_intrinsiciq`. It is intentionally not part of the default `all` chain because it is a customer-facing output layer rather than a prerequisite intelligence stage.
- The `production` profile is the canonical path for UI-ready Ask IntrinsicIQ output. `ask_intrinsiciq` must run only after `financial_pcim_validation`, `audit`, and `panel` have all completed successfully in that sequence, and a blocking `company_artifact_audit.json` status of `fail` must prevent customer-facing Ask IntrinsicIQ output from being generated.
- The underlying year-level artifacts remain `companies/<company>/<year>/financials/financial_discovery.json`, `raw_financial_tables.json`, `financial_extraction_rejections.json`, `normalized_fundamentals.json`, `financial_validation_report.json`, `financial_reconciliation_report.json`, `financial_basis_resolution.json`, `financial_fact_registry.json`, `financial_truth_reconciliation_report.json`, `financial_artifact_quarantine_report.json`, `financial_ratios.json`, `financial_growth.json`, `corporate_actions.json`, `corporate_action_rejections.json`, `shareholding_pattern.json`, `shareholding_rejections.json`, and `financial_quality_summary.json`, while the canonical company-level financial memory artifacts are `companies/<company>/company_memory/financials/financial_year_index.json`, `financial_memory_manifest.json`, `financial_truth_pack.json`, `financial_trends.json`, `financial_quality_evolution.json`, `capital_allocation_financial_timeline.json`, `ownership_evolution.json`, `financial_memory_summary.json`, `financial_quality_summary.json`, `financial_driver_attribution.json`, and `financial_memory_audit_report.json`. The truth-registry contract now explicitly distinguishes `present_direct`, `present_derived`, `partial`, `missing`, `unreliable`, `invalid`, and `not_applicable`, and it must preserve `warning_normalizations`, `precise_missing_facts`, and artifact-level quarantine findings rather than collapsing all degraded cases into broad “missing” labels. Company-memory financial warnings must pass through a contradiction gate so false “missing” claims are normalized into precise derived / unreliable / invalid wording before downstream PCIM or panel consumers see them.
- Company-level `cim` now stores full `financial_intelligence`, and company-level `pcim` now stores compact, traceable financial panel inputs plus financial source-manifest status.
- `tools/run_fundamentals_acceptance.py` is the canonical non-LLM acceptance gate for fundamentals readiness. It reads audited yearly and company-level financial artifacts, checks CIM / PCIM / panel financial integration, scores readiness, and writes `fundamentals_acceptance_report.json` plus optional markdown under `companies/<company>/audit/`.
- `financial_pcim_validation` is the canonical deterministic year-level gate for financial PCIM safety. It must verify compact financial section presence, multi-year financial memory carry-forward, no raw-table or `source_chunk` leakage, compactness, source traceability, warning propagation, forbidden-language exclusion, and deterministic value alignment before the investor panel treats financial PCIM as safe.
- `audit` now also writes `companies/<company>/audit/financial_quality_scorecard.json` plus markdown. The scorecard is deterministic, company-level, and must summarize artifact completeness, reconciliation quality, ratio/growth quality, corporate-action quality, shareholding quality, PCIM financial integration, multi-year financial memory quality, and panel financial readiness without generating new financial analysis.
- Investor Panel analysts now consume doctrine-specific compact financial PCIM sections and must interpret precomputed financial metrics rather than calculate new ratios. Saved analyst outputs must carry a structured `financial_assessment` block plus `financial_sections_consumed` / `financial_warnings_carried_forward`, and user-facing briefs may include an optional `financial_lens` when financial evidence materially informs the analyst view.
- Investor Panel evidence routing is now governed by a shared `intelligence/investor_panel/evidence_router.py` contract rather than analyst-specific claim filters. The router owns claim classification, evidence-category matching, financial-metric provenance support, section-name / JSON-filename evidence-id sanitization, routing repair, and final saved-artifact assertion across Graham, Buffett, Fisher, Munger, and Lynch.
- Saved analyst artifacts are now split into clean downstream-safe analysis plus diagnostics sidecar under the canonical company-memory panel path `companies/<company>/company_memory/investor_panel/`. Clean `<analyst>_analysis.json` must remain free of `schema_warnings`, `evidence_grounding_warnings`, `evidence_id_normalization`, `evidence_routing_diagnostics`, prompt/input/debug fields, and other internal pipeline language, while `<analyst>_analysis_diagnostics.json` preserves those repair and validation details for audit/debug use.
- Clean analyst artifact writing is now fail-soft at the string-sanitization boundary and fail-hard at true leakage boundaries. Normal narrative prose that mentions internal artifact language may be rewritten into user-safe wording or moved into diagnostics, but hard-forbidden keys (`schema_warnings`, `evidence_grounding_warnings`, `evidence_id_normalization`, prompt/input/debug fields, `source_chunk` / `raw_text` / `full_text`) plus recommendation/valuation language still block clean artifact save. When final clean validation still fails, the runner must preserve diagnostics and write a `*_analysis_failed_clean_candidate.json` sidecar for inspection.
- Investor-panel external-reader sanitation is a deterministic pre-validation boundary, not a clean-writer afterthought. `finalize_user_facing_brief_for_external_reader(...)` is the canonical brief finalizer: it must normalize brief shape, enforce pipeline-owned title/lens metadata, rewrite internal system terms (`PCIM`, artifact / JSON / evidence-id / section-label language, prompt/input wording, and similar plumbing references) into investor-readable prose across every brief field, and collect all remaining validation issues before `validate_user_facing_brief(...)` runs. The runner must also apply the same external-reader rewrite policy to active analyst prose fields such as `assessment`, `key_findings`, `red_flags`, `open_uncertainties`, `financial_*` limitation/warning lists, and nested `financial_assessment` lists so clean analyst artifacts stay human-readable while raw internal wording remains diagnostics-only.
- `committee_synthesis` now writes a required `financial_committee_view` built from analyst-level financial reasoning only. The canonical shape is `financials_used`, `basis_used`, `financial_consensus`, `financial_strengths`, `financial_concerns`, `financial_disagreements`, `missing_financial_data`, `financial_red_flags`, `financial_interpretation_limits`, and `investor_questions_from_financials`. `committee_brief` renders a `## Financial View` section from that artifact without exposing internal system language or evidence IDs by default.
- `committee_synthesis` is now a deterministic-skeleton stage rather than an LLM-owned strict-JSON stage. `build_committee_synthesis_skeleton(...)` is the canonical boundary: it owns `company`, `analysis_mode`, `generated_at`, `analysts_considered`, `missing_analysts`, `excluded_analysts`, deterministic committee ratings/confidence, `financial_committee_view`, `areas_of_agreement`, `areas_of_disagreement`, `critical_unknowns`, `financial_warning_manifest`, and analyst coverage metadata. The LLM may supply only narrative fields such as `executive_committee_summary`, `synthesis_narrative`, `disagreement_explanation`, and `what_to_watch_next`. If narrative generation fails or is malformed, the clean committee artifact must still be written from the deterministic skeleton with conservative fallback narrative, while failure details go to diagnostics. Committee financial truth must now be recomputed deterministically from hydrated financial-truth inputs and analyst truth packs before final validation so stale `fcf_missing` / `capex_missing` / `payables_missing` language cannot override derived-but-usable owner-earnings or conservative-FCF evidence.
- `panel` remains year-aware for readiness checks when invoked as `python pipelines/run_company_pipeline.py <company> <year> --stage panel`, but investor-panel analyst, committee, brief, QA, and `panel_run_summary.json` artifacts live on the canonical company-memory panel path `companies/<company>/company_memory/investor_panel/`. By default, `panel` must reuse the existing clean analyst artifacts from that directory, validate them through the same deterministic path used by `panel_doctor`, and proceed only when those saved analyst artifacts are `pass`/`warning`; analyst regeneration is explicit opt-in via `--regenerate-analysts`. The stage must not regenerate financial artifacts implicitly, must stop when the active year’s `financial_quality_summary.json` is in `fail` status, and must validate all five analysts before blocking committee synthesis.
- Final analyst status is a post-repair contract, not a raw LLM-field contract. `finalize_analyst_financial_truth_consistency(...)` is now the canonical upstream analyst financial cleanup boundary: it must reconcile every active analyst field against `analyst_financial_truth_pack`, rewrite stale broad-missing FCF / owner-earnings / capex / payables contradictions into investor-readable precision or history limits, remove raw internal financial labels from active fields, classify downstream financial warning provenance (`active_current_year_absence`, `active_multi_year_limitation`, `active_precision_limitation`, `active_basis_limitation`, `active_per_share_limitation`, `diagnostic_only_raw_warning`, `blocked_stale_warning`), and record blocked or diagnostic-only raw warning details in diagnostics before save. `finalize_analyst_validation_status(...)` must then compute `evidence_grounding_status` / `validation_status` / `status` from post-repair diagnostics, preserve clean top-level analyst status fields including `financial_truth_consistency_status`, and treat hygiene-only evidence cleanup, evidence-id normalization, weak category metadata, successfully repaired routing, stale-warning rewrites, and claims downgraded into conservative limitations/uncertainties as `warning` rather than stale `fail`. Generic boilerplate assessment filler such as “is grounded in …” or “interpreted through the doctrine focus …” is not a factual company claim and must not be evidence-grounded as an active conclusion.
- For loaded-existing analyst artifacts, panel-stage reconciliation must trust the saved finalization snapshot before any intermediate routing diagnostics. Status precedence is: `finalization_summary.hard_failures` -> `finalization_summary.active_unresolved_claims` -> `post_finalization_status.status` -> final payload `validation_status` / `evidence_grounding_status`. Raw `evidence_routing_diagnostics.unresolved_claims`, schema warnings, removed invalid IDs, or repaired routing metadata are diagnostic inputs only; they must not re-promote a saved analyst artifact from `warning` back to `fail` unless the saved finalization snapshot still shows active unresolved claims or hard failures.
- `panel_run_summary.json` is now finalized through a canonical summary pass immediately before any panel stop/go decision and immediately before write. `canonicalize_panel_run_summary(...)` must recompute per-analyst `status` / `validation_status` / `evidence_grounding_status` / `hard_failures`, rebuild `stages.analysts` from those resolved post-finalization verdicts, clear stale unresolved-claim failure strings when finalization settled at `warning`/`pass`, recompute top-level `status` / `hard_failures` / `failures`, and infer `financial_context.available=true` when saved analyst artifacts clearly used financial sections even if year-scoped readiness was not assessed. Raw artifact status and stale local arrays such as `analyst_failures` are diagnostic inputs only once post-finalization status exists.
- `panel_doctor` is the canonical non-LLM analyst-validation audit stage. It must inspect all five saved analyst outputs without fail-fast behavior, classify structural / evidence-id / routing / internal-language / financial-warning issues, read from `companies/<company>/company_memory/investor_panel/`, and write `panel_doctor_report.json` there with `scope = "company_memory"` plus the source PCIM path instead of treating a missing year as an error.
- `panel_run_summary.json` financial readiness must recognize both year-level prerequisites and the newer company-memory truth layer. `financial_context.artifacts_checked` must enumerate every candidate path inspected, `missing_artifacts` must preserve expected-but-missing paths, and `available` must become true when either company-memory financial truth exists, saved analyst artifacts clearly consumed financial sections, or the active PCIM already carries usable financial truth sections such as `financial_truth_inputs`, `analyst_financial_truth_pack`, or `financial_quality_inputs`. Optional truth-layer files may produce warnings or a `partial` status, but should not erase clearly available financial context.
- Experimental pipeline: `run_business_pipeline.py`.
- Merge target: validated work from `run_business_pipeline.py` merges into `pipelines/run_company_pipeline.py`.

## 5. Canonical Modules

| Module | Status |
| --- | --- |
| `knowledge.ai` | Testing |
| `knowledge.company_memory` | Integrated |
| `knowledge.business_blueprint` | Integrated |
| `knowledge.business_interpreter` | Integrated |
| `knowledge.business_classifier` | Integrated |
| `knowledge.question_engine` | Integrated |
| `knowledge.retrieval` | Production |
| `knowledge.module_extractor` | Integrated |
| `knowledge.discovery_runtime` | Integrated |
| `knowledge.business_understanding` | Integrated |
| `knowledge.archetypes` | Integrated |
| `knowledge.document_intake` | Production |
| `knowledge.document_identifier` | Production |
| `knowledge.financials` | Integrated |
| `knowledge.cim*` / PCIM helpers | Production |
| `scripts.smart_chunker` | Production |
| `scripts/chunker.py` | Deprecated |
| `run_business_pipeline.py` | Development |
| `scripts/embed.py` | Development |
| `scripts/chunk_and_embed.py` | Development |
| `scripts/rag.py` | Development |

`scripts/embed.py`, `scripts/chunk_and_embed.py`, and `scripts/rag.py` are experimental/manual tools only.

## 6. Current Focus

Merge validated experimental pipeline behavior into `pipelines/run_company_pipeline.py` without creating duplicate production paths.

## 7. Current Priorities

1. Plan the merge from `run_business_pipeline.py` into `pipelines/run_company_pipeline.py`.
2. Keep `scripts/smart_chunker.py` as the canonical chunker and avoid extending `scripts/chunker.py`.
3. Treat CIM / PCIM as the canonical intelligence contract.
4. Keep retrieval work inside `knowledge/retrieval`.
5. Add versioning for all AI prompts and AI output schemas before breaking changes.
6. Keep financial normalization canonical in `knowledge/financials` before normalized fundamentals and ratio/trend work expands.

## 8. Known Architectural Debt

- `run_business_pipeline.py` contains validated behavior that is not yet merged into `pipelines/run_company_pipeline.py`.
- `scripts/chunker.py` remains present but deprecated.
- Experimental embedding/RAG scripts remain as manual tools.
- Prompt and AI output schema versioning still needs implementation.
- Breaking-change version bump rules are approved but not yet enforced in code.
- Financial statement discovery, raw financial extraction, normalized fundamentals, financial validation, financial ratios, corporate actions, financial growth, shareholding-pattern tracking, company-level financial trends, company-level financial quality, and company-level financial attribution are now wired into the canonical pipeline as standalone stages, but broader interpretive financial layers are still not wired into `--stage all`.
- The new aggregate `financials` stage now writes an audit report even when deterministic downstream gates stop the run, but real-company financial runs still surface legitimate upstream quality warnings around basis clarity, capex capture, and unmapped rows that need extraction/normalization improvement rather than orchestration relaxation.
- Fundamentals acceptance QA is now available, but real companies with partial year coverage or failed yearly validation still score as untrusted for investor use. Current `datapatterns` acceptance fails because `fy22` and `fy23` have no financial artifact chain yet and `fy24` still fails yearly financial validation before ratios are available.
- Financial PCIM validation and the financial quality scorecard are now available, but real-company usefulness still depends on upstream year-level financial quality. Missing capex/FCF/share-count/payables/shareholding may remain warning-level, while misleading PCIM content, hidden hard failures, raw leakage, or invented values must still fail deterministically.
- Investor-panel financial integration is now wired into the canonical analyst path, but real-company analyst prompts can still exceed budget after compaction when large non-financial PCIM sections and new financial sections are combined. Analyst validation is intentionally strict: it must block invented financial claims, unsupported ratio/valuation language, and silent omission of major missing-data limits from the supplied financial PCIM context.
- Existing saved `committee_synthesis.json` artifacts created before the financial committee contract bump must be regenerated before the new committee brief renderer can accept them.

## 9. Active Decisions

- `pipelines/run_company_pipeline.py` is the canonical production pipeline.
- `run_business_pipeline.py` is experimental; validated behavior should merge into the canonical pipeline.
- `scripts/smart_chunker.py` is the canonical chunker; `scripts/chunker.py` is deprecated.
- `knowledge/retrieval` is canonical retrieval; embedding/RAG scripts are experimental/manual tools only.
- CIM / PCIM is the canonical intelligence contract.
- AI prompts and AI output schemas require versioning; breaking changes require version bumps.
- `knowledge/financials` is the canonical fundamentals schema subsystem, and its yearly plus company-level artifacts now feed CIM / PCIM directly.
- Financial monetary values must preserve original reported value/unit and normalize to `value_crore` in INR.
- Financial schema / validator layers must not contain LLM-calculated ratios or downstream analytical math.
- `financial_discovery` is the canonical non-LLM stage for locating likely financial statement and note sections before any financial number extraction.
- `financial_discovery` items must carry explicit basis metadata (`basis`, `basis_confidence`) whenever standalone / consolidated evidence is detectable so downstream stages do not need to re-guess basis from raw text alone.
- `financial_discovery` must classify primary statements, explicit notes, accounting-policy text, auditor-report text, management-discussion financial summaries, and irrelevant financial narrative separately so downstream extraction can favor precision over broad recall.
- `financial_extraction` is the canonical non-LLM stage for capturing raw financial table rows with preserved labels, raw values, basis hints, typed values (`value_type`, `raw_number`), and `value_crore` normalization only where the row is genuinely monetary.
- Financial unit resolution must treat an explicit table-wide monetary declaration (for example, “all figures are in INR crores unless specifically stated otherwise”) as the default even when the same table contains percentage cells. Explicit `%` value tokens remain row/value-level percentage overrides; their presence must not retype unrelated monetary rows or erase their crore conversion.
- `financial_extraction` must also emit `financial_extraction_rejections.json` so rejected table candidates remain auditable instead of disappearing silently.
- The failure-learning registry now tracks both root-cause status and remediation status. Guardrails that were added in response to an incident should be reflected there so future runs can tell the difference between a documented failure and an implemented safeguard.
- `financial_normalization` is the canonical non-LLM stage for mapping raw financial rows into field-level normalized fundamentals, preserving source lineage, basis, original reported values, and unmapped rows.
- `normalized_fundamentals.json` is the canonical preferred-basis surface for downstream math. It must expose `preferred_basis`, `basis_options_available`, `selected_basis_reason`, `basis_confidence`, and `basis_manifest`, must preserve non-preferred evidence under `basis_views`, and must not silently promote standalone values into a consolidated preferred view or vice versa.
- `financial_normalization` must prefer primary statements over note summaries or management-discussion financial summaries when multiple candidate rows compete for the same canonical field.
- `cash_flow.capex` is a guarded normalized field: it should only be populated from clear investing cash-flow capex evidence or equally explicit capex/additions evidence, must preserve cash-flow sign semantics, and should carry `capex_abs_crore` plus `sign_convention` metadata so downstream FCF remains deterministic. Missing capex is acceptable as a warning; incorrect capex sourcing is not.
- `financial_validation` is the canonical non-LLM readiness gate that checks normalized fundamentals for required fields, consistency, unit/source integrity, and suspicious sign conventions before ratios are allowed downstream.
- `financial_reconciliation` is the canonical non-LLM gate between `financial_validation` and downstream financial math. It verifies that normalized fundamentals still point to relevant source lines, preserve the correct value type for EPS/share-count fields, and do not rely on unrelated or disallowed source sections before ratios or growth proceed.
- `cash_flow.fcf` should normally be derived from CFO plus capex provenance rather than loosely extracted from noisy text. Missing FCF because capex is genuinely unavailable is a warning path; populated FCF from an unrelated heading, balance row, or non-capex source is a hard failure.
- `financial_ratios` is the canonical non-LLM ratio-calculation stage that reads reconciled normalized fundamentals, preserves formula plus input provenance, emits null-plus-warning for missing inputs, and refuses to calculate on top of failed reconciliation for critical inputs.
- `financial_ratios` and `financial_growth` must carry explicit basis context (`basis_used`, `basis_confidence`, `basis_warnings`) so downstream consumers can distinguish a clean consolidated/standalone run from an explicitly unknown or warning-level basis state.
- `financial_quality` is the canonical non-LLM diagnostic stage with two deterministic modes. Year mode reads `normalized_fundamentals.json`, `financial_reconciliation_report.json`, `financial_validation_report.json`, `financial_ratios.json`, `financial_growth.json`, `corporate_actions.json`, and optional `shareholding_pattern.json`, then writes a year-level `financial_quality_summary.json` with section-level assessments, evidence metrics, warnings, limitations, red flags, missing-data notes, and investor questions. Company-memory mode reads `financial_trends.json`, converts multi-year growth, margin, return, cash-conversion, leverage, working-capital, ownership, and corporate-action signals into investor-readable positives, red flags, missing-data notes, and warning-aware quality sections, and writes the company-memory `financial_quality_summary.json` without valuation or recommendation language.
- `financial_attribution` is the canonical non-LLM company-level attribution stage that reads `financial_trends.json`, `financial_quality_summary.json`, and available multi-year company-memory artifacts, then links major financial movements to possible business events or management actions without overclaiming causation, and writes `financial_driver_attribution.json`.
- `corporate_actions` is the canonical non-LLM metadata stage for dividends, split/bonus/dilution events, face-value changes, and share-count comparability warnings, using normalized fundamentals plus raw financial table evidence without silently restating historical per-share values.
- `corporate_actions` must stay conservative: dividend cash outflow and dividend-per-share are separate signals, share counts must never be stored as `amount_crore`, QIP/rights/preferential issue rows must distinguish issue events from proceeds-utilization rows where the source makes that distinction visible, and rejected or nullified action-like candidates must remain auditable in `corporate_action_rejections.json` instead of being silently accepted or dropped.
- `financial_growth` is the canonical non-LLM growth-calculation stage that reads reconciled normalized fundamentals plus available ratio history, computes YoY and multi-year growth where evidence exists, records null-plus-warning when history is missing or unusable, and may derive margin changes directly from normalized fundamentals when ratio history is unavailable.
- `shareholding_pattern` is the canonical non-LLM ownership-tracking stage that reads normalized fundamentals plus available raw shareholding rows, records ownership categories and change signals only from explicit shareholding evidence, emits warning-only artifacts when real reports do not yet yield usable ownership rows, and preserves rejected ownership candidates in `shareholding_rejections.json` for auditability.
- `financial_trends` is the canonical non-LLM company-level aggregation stage that reads yearly financial artifacts, preserves year-level provenance plus comparability warnings, and builds the reusable multi-year financial trend view before downstream CIM / PCIM interpretation.
- `financials` is the canonical aggregate year-level fundamentals stage. It must run the deterministic yearly fundamentals stages in order, must still emit `financial_audit_report.json` when a downstream readiness gate fails, and must not bypass validation in order to produce ratios or growth.
- `financial_memory` is the canonical aggregate company-level fundamentals stage. It must run `financial_trends`, `financial_quality`, and `financial_attribution` in order, then derive `financial_year_index`, `financial_quality_evolution`, `capital_allocation_financial_timeline`, `ownership_evolution`, and `financial_memory_summary` before emitting `financial_memory_audit_report.json` for downstream CIM / PCIM consumption.
- `financial_memory` now also owns `financial_memory_manifest.json` and `financial_truth_pack.json`. These are the canonical machine-readable company-memory surfaces for year coverage, truth-registry readiness, blocked-vs-allowed downstream warnings, precise missing metrics, unreliable metrics, invalid/quarantined metrics, and investor-relevant follow-up questions. Company-memory financial summaries must not override year-level direct or derived truth facts.
- `financial_truth_pack.json` is now also the canonical hydration boundary between deterministic financial memory and downstream panel reasoning. It must record `source_files_checked`, `source_files_used`, `source_files_missing`, `usable_current_metrics`, `usable_derived_metrics`, `partial_metrics`, `derived_not_explicitly_reported`, `trend_durability_limits`, `precision_limits`, warning-policy buckets (`financial_warnings_allowed_downstream`, `financial_warnings_blocked_downstream`, `financial_warnings_rewritten`), and panel-readiness fields (`financial_panel_status`, `financial_panel_status_reason`, `financial_panel_usable_domains`, `financial_panel_limited_domains`, `financial_panel_blocked_domains`, `source_provenance`). If investor financial modules exist but hydration fails to populate the truth-pack sections they are responsible for, the truth pack must mark `financial_panel_status = invalid` rather than silently degrading to broad missing-data claims.
- `investor_financials` is the canonical deterministic investor-facing financial module stage built on company-memory financial truth. It must write `owner_earnings_bridge.json`, `capital_allocation_roi_ledger.json`, `working_capital_quality_drilldown.json`, `order_revenue_cash_conversion_tracker.json`, `per_share_compounding_analysis.json`, and `investor_financial_modules_manifest.json` under `companies/<company>/company_memory/financials/investor_financial_modules/`. Those modules may use reconciled truth facts, truth-pack warning policy, trends, quality summary, driver attribution, and corporate-action/shareholding metadata, but they must not use raw `source_chunk` text, stale broad missing-data wording, or LLM synthesis.
- Derived per-share metrics inside `per_share_compounding_analysis.json` and hydrated `financial_truth_pack.json` must carry explicit unit metadata (`unit`, numerator/denominator metric names and units, share-count basis, calculation formula). If a numerator is in INR crore and the denominator is an absolute share count, the only valid generic conversion is `value_crore * 10000000 / shares`. Directly reported per-share metrics such as EPS, dividend per share, and book value per share remain `reported_directly` and must not be re-converted as if they were crore values. `financial_truth_pack.json` now records `unit_validation_warnings`, `unit_validation_failures`, and `recomputed_metrics` so downstream consumers can distinguish repaired closing-share estimates from quarantined unit bugs.
- Null per-share placeholders are not usable financial truth. When a derived per-share metric lacks either numerator or denominator inputs, `financial_truth_pack.json` must not place it into `usable_current_metrics` or mark it `present_derived`; instead it must classify the row as `precise_missing` or `partial` with `missing_inputs`, and it may emit the closing-share precision warning only for rows that were actually calculated from closing shares.
- Derived per-share hydration must use a shared share-count resolver across `fcf_per_share`, `owner_earnings_per_share`, `cfo_per_share`, `revenue_per_share`, and future derived per-share metrics. The resolver priority is weighted-average basic shares first, weighted-average diluted shares when a diluted basis is explicitly required, then closing shares as a fallback with a precision warning. If numerator and resolved share count both exist, the metric should be calculated; if either side is truly absent, it must be classified as missing or partial rather than emitted as a misleading fake usable row.
- `financial_pcim_validation` is the canonical year-level deterministic safety gate for financial PCIM. It must validate existing PCIM output against audited year-level financial artifacts, must not call LLMs, must not calculate new ratios, and must fail on raw leakage, invented financial values, hidden hard failures, or forbidden valuation/recommendation language.
- `run_fundamentals_acceptance.py` is the canonical final QA gate for fundamentals readiness. It must stay deterministic, must not call LLMs, must treat missing revenue/PAT, failed financial validation, missing normalized INR-crore values, missing core traceability, or missing PCIM financial manifest as hard failures, and must keep `ready_for_valuation` false at this stage even when `ready_for_panel` becomes true.
- `cim` is the canonical storage layer for full financial intelligence artifacts, including yearly fundamentals, yearly ratios/growth/corporate-actions/shareholding, and company-level financial trends, quality summary, and driver attribution.
- `cim.financials` is the canonical compact financial judgment view for downstream consumers. It must expose named sections for `core_fundamentals`, `profitability`, `growth`, `return_on_capital`, `cash_conversion`, `balance_sheet`, `working_capital`, `per_share`, `corporate_actions`, `shareholding`, `financial_quality_summary`, plus `basis_used`, `basis_confidence`, `warnings`, `limitations`, and `source_manifest`.
- `pcim` is the canonical compact panel-facing projection layer for financial evidence. It must preserve source-year and source-artifact traceability, exclude `source_chunk`, and carry `pcim_source_manifest` financial status (`financial_artifacts_used`, `financial_years_covered`, `financial_status`, `financial_warnings`).
- The canonical compact financial PCIM sections are `financial_fundamentals_inputs`, `financial_trend_inputs`, `financial_growth_inputs`, `profitability_inputs`, `cash_conversion_inputs`, `return_on_capital_inputs`, `balance_sheet_strength_inputs`, `working_capital_inputs`, `per_share_inputs`, `corporate_action_inputs`, `ownership_inputs`, `financial_quality_inputs`, `financial_driver_inputs`, and `multi_year_financial_inputs`. These sections must stay compact, deterministic, raw-table-free, and panel-safe.
- `multi_year_financial_inputs` is the canonical panel-facing summary of company-level financial memory. It must be derived only from audited year-level financial artifacts and company-level financial memory files, must stay compact, must exclude raw tables and `source_chunk`, and should be the preferred multi-year financial context surface for downstream investor-panel reasoning when present.
- `financial_quality_scorecard` is the canonical deterministic company-level financial readiness scorecard. It must aggregate year-level financial PCIM validation, audited yearly financial artifacts, multi-year financial memory, and panel-readiness signals into dimension scores and recommended next fixes, but it must not perform new financial interpretation or valuation.
- `financial_source_manifest` and `financial_panel_ready` are required top-level PCIM financial contract fields whenever financial PCIM sections are present. `financial_panel_ready` may be true only when usable financial quality evidence exists and manifest status is `pass` or `warning`.
- Financial numbers enter downstream CIM / PCIM and investor workflows only through audited financial memory: normalized INR-crore values, preserved original values/units, source traceability, and no-LLM-math checks are required contract points.
- The financial truth registry is the canonical generic classification layer for capex, FCF, working-capital, debt, and share-count readiness. It may derive FCF, payable-days, cash-conversion-cycle, net debt, and capex sub-buckets when deterministic inputs exist; it must preserve provenance and warning normalization, and it must prefer `partial` or `unreliable` over broad `missing` when usable but limited evidence exists.
- Investor-panel and committee financial reasoning must now prefer reconciled financial truth sections when they exist in PCIM. The preferred upstream surfaces are `financial_truth_inputs`, `financial_snapshot_inputs`, `financial_quality_inputs`, `owner_earnings_readiness_inputs`, `working_capital_quality_inputs`, `capital_allocation_financial_inputs`, `per_share_compounding_inputs`, `unreliable_financial_inputs`, `invalid_or_quarantined_financial_inputs`, `precise_missing_financial_inputs`, `financial_warning_policy`, `financial_panel_status`, `financial_panel_usable_domains`, `financial_panel_limited_domains`, `financial_panel_blocked_domains`, and `investor_financial_questions`. Fallback to older compact financial sections is allowed only if it does not reintroduce broad false warnings.
- `analyst_financial_truth_pack` is the canonical downstream investor-panel contract for financial truth ingestion. It must distinguish `usable_current_metrics`, `usable_derived_metrics`, `partial_metrics`, `unreliable_metrics`, `invalid_or_quarantined_metrics`, `precise_missing_metrics`, `trend_durability_limits`, `precision_limits`, `allowed_financial_warnings`, `blocked_financial_warnings`, `rewritten_financial_warnings`, `investor_financial_questions`, `financial_panel_status`, and `source_provenance`. Analysts must not emit any warning present in `blocked_financial_warnings`; they must instead use the corresponding precise or rewritten limitation.
- Clean analyst artifacts now preserve richer financial truth classes for downstream committee use: `precise_missing_financial_data`, `derived_not_explicitly_reported`, `partial_financial_data`, `unreliable_financial_data`, `invalid_or_quarantined_financial_data`, `trend_durability_limits`, and `financial_questions_for_investor`. Committee aggregation may form consensus only from allowed financial warnings and those explicit truth classes; blocked stale warnings must never become committee consensus.
- Active stale financial warnings are diagnostics-only once reconciled truth says the underlying metric is usable. If current usable financial truth shows FCF/owner-earnings, identified capex, or payables/payable-days are available, clean analyst artifacts, `committee_synthesis.json`, and `committee_brief.md` must rewrite or drop the stale missing-data wording and keep the original blocked wording only in diagnostics.
- Clean analyst artifacts must now preserve their final status snapshot for downstream reuse. Saved `<analyst>_analysis.json` files may keep `status`, `validation_status`, `evidence_grounding_status`, `financial_truth_consistency_status`, clean warning lists, and user-safe summaries that blocked stale or internal raw financial warnings were handled, while raw blocked-warning text and internal-label details stay only in `<analyst>_analysis_diagnostics.json`.
- `per_share_compounding_analysis.json` is the canonical per-share truth module for investor reasoning. `fcf_per_share` must be expressed in `INR/share`, converting `value_crore` numerators with absolute share-count denominators via `value_crore * 10_000_000 / shares`, and it must carry numerator/denominator units, a calculation formula, and a precision warning whenever closing shares are used instead of weighted-average shares.
- Investor-panel doctrine mappings are the canonical control for which compact financial PCIM sections each analyst can see. Analysts may interpret only precomputed metrics already present in PCIM, must surface missing-data and comparability limits explicitly, and must not calculate new ratios in the LLM layer.
- Investor-panel financial metric validation must use a deterministic selected-metric registry derived from the exact compact financial PCIM sections shown to the analyst. `financial_metrics_used` should prefer structured `{metric_id, metric, period, used_for}` objects, while string fallbacks may be canonicalized only through approved metric aliases tied to selected PCIM evidence.
- Investor-panel `financial_metrics_used` validation now uses the union of (a) canonical metrics present in selected compact financial PCIM sections, (b) hydrated financial-truth / warning-policy sections, and (c) compact investor financial module summaries that are actually included for the analyst. Deterministic aliases such as owner-earnings, identified-capex, capex-deployed, closing-shares, receivables/inventory/payables value labels, working-capital day labels, and conservative-FCF labels must normalize to canonical metric IDs before validation. Unsupported metric references must be removed from the clean analyst artifact, preserved in diagnostics, and surfaced as schema warnings rather than silently accepted or falsely validated against only the older thin PCIM metric list.
- Investor-panel analyst output now carries `schema_warnings` as a canonical validation artifact. Harmless list-shape drift in fields such as `reasoning_limits`, `financial_missing_data`, `financial_interpretation_limits`, `financial_warnings_carried_forward`, `financial_red_flags`, and nested `financial_assessment` list fields may be normalized deterministically into `list[str]`, but only with explicit `schema_warnings`; raw payload leakage (`source_chunk`, `raw_text`, `full_text`), forbidden recommendation/valuation language, unsupported financial claims, and missing required sections must still fail hard.
- Investor-panel LLM output is canonicalized through a repair-first boundary before strict validation. The runner must parse raw JSON, build a deterministic analyst skeleton, merge draft content into that skeleton, repair malformed `assessment` / `financial_assessment` / `user_facing_brief` / list / enum fields, move unknown fields to diagnostics, then run financial-warning carry-forward, evidence normalization, and strict validation. `_validate_llm_panel_output` must never validate raw unnormalized LLM output directly.
- `user_facing_brief` canonical metadata is pipeline-owned, not LLM-owned. Before `validate_user_facing_brief(...)` runs, the runner must finalize the brief into a canonical object and deterministically enforce the analyst title from `LENS_CONFIG` plus any canonical static lens text defined there. LLM-provided brief prose may fill explanatory fields such as bullets, bottom line, and optional financial lens, but canonical title/lens mismatches must be overwritten and recorded in diagnostics rather than failing as raw LLM drift.
- Investor-panel analyst `evidence_ids` must contain only canonical evidence IDs present in the PCIM evidence lookup. PCIM section-name aliases such as `risk_inputs`, `financial_quality_inputs`, `governance_and_incentive_inputs`, and other compact-section labels are not evidence IDs; they must be removed from every nested analyst evidence list before save, recorded under `evidence_id_normalization.removed_invalid_ids`, and leave the saved analyst output at `warning` rather than `fail` when the cleanup is hygiene-only. Unknown evidence IDs are likewise removed from active evidence lists but retained in diagnostics; a final recursive assertion must prevent invalid evidence IDs from being written.
- Munger analyst outputs carry internal `munger_evidence_routing_diagnostics` so deterministic pre-validation routing repairs remain auditable. Governance/incentive claims may not cite market-risk / FX / interest-rate evidence unless the claim is explicitly about risk oversight or risk governance; misrouted governance claims must be rerouted to governance/ownership/capital-allocation/corporate-action/uncertainty evidence when such evidence is available to Munger, or converted into limitation / open-uncertainty language before final grounding validation. These diagnostics are internal and must not appear in user-facing briefs.
- Financial warning carry-forward is a canonical investor-panel validation contract. Major financial warnings from selected PCIM context must be preserved semantically in analyst output via `financial_missing_data`, `financial_interpretation_limits`, or `financial_warnings_carried_forward`, with precise wording preferred over generic placeholders. The system must build deterministic required warning groups from selected financial PCIM context, auto-carry any omitted major warning before final validation, record that repair in `schema_warnings`, and still fail hard if the analyst contradicts the required warning (for example by claiming FCF, owner earnings, or FCF-backed dividend quality when FCF/capex is missing). In particular, share-count warnings must distinguish `share count missing` from `weighted average shares missing` / `diluted shares missing`, and generic missing-data claims must not be emitted when a more specific supported limitation is available.
- Investor-panel prompt compaction is now a hard serialized-section-budget contract, not only a best-effort global prompt shrink. After recursive compaction, each included PCIM section must still obey deterministic post-compaction char caps (`multi_year_inputs` 4000; `financial_quality_inputs`, `capital_allocation_inputs`, `management_quality_inputs`, `moat_inputs`, `governance_and_incentive_inputs` 3000; `business_understanding` and `business_economics_inputs` 2500; other sections 2500). `multi_year_inputs` must preserve `years_covered`, `limitations`, compact evidence traceability, and no `source_chunk`, while section-over-budget fallbacks must collapse to limitation objects rather than silently leaking oversized payloads into analyst prompts.
- Investor-panel prompt packing is now doctrine-aware, class-budgeted, and hard-gated on the full prompt rather than only broad section caps. The canonical default budgets are `total_prompt_budget_tokens = 9000`, `hard_max_prompt_tokens = 10000`, `compact_input_pack_budget_tokens = 4200`, `financial_truth_pack_budget_tokens = 900`, `evidence_pack_budget_tokens = 500`, `doctrine_context_budget_tokens = 2200`, and the remaining budget is reserved for instructions/schema and warning-policy carry-forward. The runner must track token usage separately for `system_instructions`, `analyst_doctrine_instructions`, `output_schema_or_contract`, `compact_pcim_input_pack`, `compact_financial_truth_pack`, `compact_evidence_pack`, and `warning_policy_pack`. It must compact `financial_truth_inputs` into an analyst-specific financial-truth digest rather than dumping the full truth-pack structure, must keep the evidence payload to a subset or IDs-only fallback, must prioritize doctrine-relevant sections first, may summarize lower-priority requested sections under `available_but_not_included_due_budget`, and must fall back to an emergency skeleton prompt rather than failing immediately when normal compaction cannot fit. The runner must emit per-analyst prompt-budget diagnostics at `companies/<company>/company_memory/investor_panel/prompt_budget_diagnostics_<analyst>.json` including raw/compacted section token counts, included/excluded sections, shrink passes, emergency-mode flag, sections dropped due budget, final prompt/pack tokens, token-usage-by-budget-class, evidence/financial-truth token slices, omitted-evidence count, and final budget status.
- Committee synthesis may interpret only analyst-level financial outputs already produced by the investor panel. It must not calculate new ratios, must not introduce unsupported financial claims, and must keep user-facing committee brief text free of internal architecture terms such as PCIM / CIM / schema / validator language. Committee financial synthesis must derive from analyst `financial_assessment`, `financial_sections_consumed`, `financial_warnings_carried_forward`, and other saved analyst financial fields rather than raw PCIM or raw financial artifacts.
- Committee synthesis `critical_unknowns` use a canonical grounded shape: `unknown`, `raised_by`, `why_it_matters`, `source_uncertainty_ids`, and `evidence_limit`. Every critical unknown must trace to the analyst uncertainty registry; missing `raised_by` or `source_uncertainty_ids` may be deterministically inferred only from that registry, and ungrounded unknowns must fail validation.
- Committee synthesis clean artifacts must remain downstream-safe. User-facing `committee_synthesis.json` may not expose internal normalization/debug markers such as `grounding_status`, prompt/input-pack fields, schema-repair metadata, or other internal pipeline terms. Those belong only in `committee_synthesis_diagnostics.json`.
- Committee evidence-quality notes may disclose that an analyst was excluded because cited source references could not be verified, but raw evidence IDs, JSON paths, validator reason codes, and serialized diagnostic payloads must be rewritten at the final brief boundary and remain blocked by the downstream forbidden-language gate.
- Committee brief rendering has a final canonical brief-view boundary before write. The renderer must first run a truth-aware finalizer over `committee_synthesis.json`, then build a canonical `brief_view` object (`committee_view`, `analyst_agreements`, `analyst_disagreements`, `financial_view`, `strongest_positive_signals`, `most_important_risks`, `critical_unknowns`, `investigation_questions`, `evidence_quality_notes`, `synthesis_limits`), validate that object against `committee_financial_truth`, and render markdown only from that brief view. After `brief_view` is built, raw `committee_synthesis` lists must not be used directly by the markdown writer. That same truth-aware finalizer is also part of the clean committee artifact boundary: once deterministic financial truth says FCF/capex/payables are usable, active `committee_synthesis.json` fields such as `overall_committee_view.summary`, `synthesis_narrative`, `what_to_watch_next`, `most_important_risks`, `critical_unknowns`, `investigation_questions`, and `financial_committee_view` must be rewritten or dropped rather than leaving stale missing-data contradictions in the saved artifact. The canonical `financial_view` now distinguishes `missing_financial_data` from `precision_limited_financial_data`, and the user-facing brief must classify only real positive evidence under Financial Strengths while routing basis/share-count/capex-split/multi-year-bridge gaps into specific “Missing / Incomplete Inputs” or other limitation sections without semantic duplication. The brief-view validator must reject stale FCF/capex/payables contradictions, ellipsis or dangling-fragment truncation, semantic truncation without ellipsis (for example dangling `and` / `with` / unmatched parentheses), incomplete currency fragments, duplicate critical unknowns, blank/non-question investigation prompts, and empty `supported_by` signal attributions. `committee_brief_qa` is the canonical user-facing gate for contradiction checks against that same canonical brief-view source, raw internal-language detection, broken formatting fragments, blank or malformed investigation questions, weak financial-strength sections when richer financial truth is available, and business-only text leaking into rendered Financial Strengths. Readability/classification problems such as duplicated financial limitations, limitation text inside Financial Strengths, or omission of severe working-capital risk from the Committee View / Dominant Tension should surface as warnings when the artifact remains structurally sound; contradictions, broken truncation, forbidden language, and missing required sections remain hard failures.
- The panel-stage financial readiness gate is orchestration-only: it may inspect the presence and status of yearly financial artifacts, surface missing or warning-level finance context into the panel summary, and rely on CIM / PCIM rebuilds to carry those limitations forward, but it must not silently rerun financial discovery/extraction/normalization/ratios/growth stages.
- The panel-stage `financial_context` summary is company-memory aware even without an active year context. It must report checked company-memory financial truth sources, may treat analyst/committee financial usage as evidence of availability, and should use `missing` only when no usable financial source is detected at all.

## 4b. Document Intake Contract

- `knowledge/document_intake.py` owns the Phase 1 contract: `DocumentIntakeManifest`, `FileIdentity`, `CompanyIdentity`, `DocumentIdentity`, `ReportingPeriod`, `DocumentDates`, `ClassificationState`, `IntakeProvenance`, all enums (`SourceType`, `EntityScope`, `ConfidenceLevel`, `ClassificationStatus`, `DetectionMethod`, `FiscalQuarter`), and the compatibility adapters `build_legacy_annual_report_manifest(...)` and `manifest_to_legacy_pipeline_inputs(...)`. Phase 1 status: `DOCUMENT_INTAKE_CONTRACT_CLOSED`.
- `knowledge/document_identifier.py` is the Phase 2 canonical entry point: `identify_document(path) -> DocumentIntakeManifest`. It implements 11 explicit pipeline stages: file validation → content hash → content probe (first 5 pages) → company identification → source-type classification → reporting period detection → document date extraction → entity scope detection → language detection → confidence engine → manifest construction. Filename is evidence at most, never identity authority. Company identification uses a dynamic registry built from the `companies/` directory without company-specific rules. No LLM dependency. Identifier version: `document_identifier.v2`. Phase 2.1 repaired the reporting-period precedence: the Indian FY range stage (`_select_best_fy_candidate`) now scores all candidate matches by semantic context strength — filing serial numbers (slash-heavy paths) score −100 and are de-ranked below any genuine reporting-period phrase (Annual Report, Financial Year, Year Ended → +10); bare year ranges score 0 and remain usable when no stronger evidence exists. The helper functions `_is_filing_reference`, `_score_fy_candidate`, and `_select_best_fy_candidate` are the canonical period-ranking primitives.
- Classification states: `IDENTIFIED` (company resolved, source type known, periodic report has fiscal year), `REVIEW_REQUIRED` (company found but some fields unresolved, contradictory signals, OR company name detected in document but not mapped to a canonical registry key), `UNIDENTIFIED` (no company name detected anywhere in the document), `REJECTED` (no extractable text, corrupt file, unsupported type, empty file).
- Cross-company proof: `identify_document` correctly identifies ujjivan from its real PDF and polymatech from a synthetic text fixture without any company-specific rules. Opaque filenames (`document_001.pdf`) and misleading filenames (another company's name) return the same company key as the correctly named file when document content is identical.
- The compatibility adapter is available through Phase 1: `manifest_to_legacy_pipeline_inputs(manifest)` converts an `IDENTIFIED` annual-report manifest into `LegacyPipelineInputs(company, year, source_file)` for the existing pipeline without code duplication.
- Phase 3 (2026-09-01) added the canonical source router and generic processing entry point. `knowledge/document_router.py` owns `RouteStatus` (ROUTABLE / REVIEW_REQUIRED / UNSUPPORTED / REJECTED), `ProcessorState` (AVAILABLE / NOT_IMPLEMENTED / UNAVAILABLE), `RoutingDecision`, and `SourceRouter`. `knowledge/document_processor.py` owns `ProcessorInterface` (abstract base), `AnnualReportProcessor` (Phase 3 concrete implementation via legacy adapter), `DocumentProcessingResult`, `ProcessorStatus`, error taxonomy (`DocumentIntakeError`, `IdentificationUnresolvedError`, `ReviewRequiredError`, `UnsupportedSourceTypeError`, `ProcessorUnavailableError`, `CompatibilityAdapterError`, `ProcessorExecutionError`), and `process_document(path, *, execute=False)` — the canonical generic entry point. `pipelines/process_document.py` is the minimal CLI.
- Phase 3.3 (2026-09-02) implemented the two-axis document composition model. `source_type` in `DocumentIdentity` now represents the payload (what the document IS), and a new `source_channel: SourceChannel` field captures the distribution envelope (how it was delivered). `SourceChannel` values: DIRECT, EXCHANGE_FILING, REGULATORY_PORTAL, UNKNOWN. `SourceType` was extended with EARNINGS_RELEASE and EXCHANGE_DISCLOSURE. `document_identifier.v3` adds: (a) an adaptive probe — page 1 is scanned for exchange-filing wrapper signals (`_detect_exchange_wrapper`), and when a wrapper is found, classification runs on pages 2+ only so the Regulation 34 cover letter does not override the substantive payload type; (b) `_extract_legal_company_name` extracts the corporate legal name from document text even when the company is not in the registry; (c) detected-but-unmapped companies now produce REVIEW_REQUIRED (not UNIDENTIFIED), with `detected_legal_name` preserved in `CompanyIdentity`. Reporting period and entity scope detection also run on the payload probe when a wrapper is active, preventing cover-letter filing dates from contradicting payload dates. The router continues to route on `source_type` (the payload), so an ANNUAL_REPORT wrapped in an EXCHANGE_FILING channel is now correctly ROUTABLE rather than UNSUPPORTED. Proof: the 437-page LTTS annual report (a real BSE/NSE submission previously blocked as EXCHANGE_FILING/UNIDENTIFIED) now correctly produces `source_channel=EXCHANGE_FILING, source_type=ANNUAL_REPORT, status=REVIEW_REQUIRED, detected_legal_name="L&T Technology Services Limited"`. Ujjivan FY25 and Sun Pharma FY26 production PDFs now route as ANNUAL_REPORT (previously EXCHANGE_FILING/UNSUPPORTED). Phase 3 status: `DOCUMENT_COMPOSITION_CONTRACT_CLOSED`. ENG-068 closed.
- Canonical flow: RAW FILE → `identify_document(path)` → `DocumentIntakeManifest` → `SourceRouter.route(manifest)` → `RoutingDecision` → `ProcessorInterface.process(manifest, path)` → `DocumentProcessingResult`.
- Routing rules: REJECTED manifest → RouteStatus.REJECTED (no processor invoked). UNIDENTIFIED → RouteStatus.UNSUPPORTED. REVIEW_REQUIRED → RouteStatus.REVIEW_REQUIRED (operator must intervene). IDENTIFIED + ANNUAL_REPORT → RouteStatus.ROUTABLE. IDENTIFIED + any other source type → RouteStatus.UNSUPPORTED with processor=NOT_IMPLEMENTED. Identification success is explicitly separated from processing support.
- Source-type routing table: ANNUAL_REPORT → AVAILABLE (via AnnualReportProcessor + legacy adapter). QUARTERLY_REPORT → AVAILABLE after Phase 4 Gate B production proof. INVESTOR_PRESENTATION → AVAILABLE after Phase 5 production proof. EARNINGS_CALL_TRANSCRIPT → AVAILABLE after Phase 6.1 real-transcript production proof. EARNINGS_RELEASE → AVAILABLE after Phase 7 real-release production proof (LTTS Q1 FY27, 6965ca6d). EXCHANGE_FILING, EXCHANGE_DISCLOSURE, OTHER, UNKNOWN → NOT_IMPLEMENTED. Non-annual-report source types never fall back to the annual-report pipeline.
- Destination is computed only after identity is resolved: `companies/<company>/<year>/raw/<filename>`. Files are never moved by default. `execute=False` is the safe default; execution requires explicit intent.
- Phase 4 (2026-09-02) audit returned `BLOCKED_REAL_QUARTERLY_DOCUMENT_MISSING`. Architecture contract for `QuarterlyReportProcessor` is fully established in `knowledge/document_processor.py`: `QuarterlyProcessingResult` dataclass and `QuarterlyReportProcessor` stub are present but the processor is NOT registered in `_PROCESSOR_REGISTRY` and the router remains `QUARTERLY_REPORT → NOT_IMPLEMENTED`. Audit findings: (1) No quarterly-results PDF exists anywhere in the repository. (2) `CompanyContext` has no `quarter` field; storage is `companies/<company>/<year>/` only. (3) The pipeline is `company + year` scoped with no quarterly stage path. Architecture contract: storage path `companies/<company>/<fy>/quarters/<q>/`, `CompanyContext` needs a `quarter: Optional[str]` field, quarterly evidence must feed the common intelligence path (not a separate quarterly universe), and current-quarter vs YTD vs comparative column disambiguation must be implemented in financial extraction before the processor can be validated. See ENG-069 for the six-step unblocking checklist.
- Phase 4.1 (2026-09-02) audit returned `BLOCKED_REAL_QUARTERLY_DOCUMENT_INVALID`. Gate A: The supplied Tanla PDF (`3e52b313-f8d6-4893-b039-88b5b8f070f6.pdf`) is an INVESTOR_PRESENTATION (Tanla "Investor Update Full Year & Q4 FY26", filed with BSE/NSE under Regulation 30 as a slide-deck), NOT a QUARTERLY_REPORT (Regulation 33 SEBI LODR formal quarterly results filing). Gate A fails; Gate B (QuarterlyReportProcessor full implementation) remains BLOCKED. Two root-cause classifier defects were found and fixed generically: (1) `_EXCHANGE_WRAPPER_SIGNALS` now includes `\b(enclosing|attaching|submitting)\s+herewith\b` (+4) and `\bnational\s+stock\s+exchange\b` (+3), so cover letters that say "enclosing herewith" (common in Indian exchange filings) correctly activate wrapper detection. (2) `_PRESENTATION_SIGNALS` now includes `\binvestor\s+update\b` (+6); the ambiguous `q[1-4]\s+(fy)?\d{2,4}\s+results?\b` quarterly signal was downgraded from +6 → +4 so investor-update slide decks no longer tie with QUARTERLY_REPORT. Genuine quarterly filings still classify correctly via "quarterly results" (+5) + "quarter ended" (+4) + "unaudited" (+3) + "limited review" (+3). Three regression tests added; full suite: 74 passed, 0 failures.
- Phase 6.1 (2026-09-02) closed real transcript production validation. The supplied opaque PDF identified from content as a Tanla Q4 FY26 earnings-call transcript (`source_channel=EXCHANGE_FILING`, `source_type=EARNINGS_CALL_TRANSCRIPT`, `fiscal_year=fy26`, `fiscal_quarter=Q4`). Direct processing before registration produced 114 speaker turns, 53 management turns, 21 analyst questions, 266 management-claim records, 13 page chunks, and 266 `TRANSCRIPT`-authority common-evidence records under `companies/<company>/<fy>/earnings_calls/<hash>/`. The production gate exposed and fixed one shared parser defect: real PDFs may render bullet markers on separate lines from participant names, and exchange-cover metadata labels such as `Date:` / `Sub:` must not become speaker turns. After the fix, speaker attribution preserves MANAGEMENT / ANALYST / MODERATOR roles, Q&A links management answers to analyst questions, claim records carry source period, optional target period, page/turn provenance, qualifier language, and transcript financial speech remains lower-authority evidence. Misleading filename proof confirmed identity is content-based. Router now declares `EARNINGS_CALL_TRANSCRIPT → AVAILABLE`, and `_PROCESSOR_REGISTRY` includes `EarningsCallTranscriptProcessor`. Status: `EARNINGS_CALL_TRANSCRIPT_PROCESSOR_CLOSED`.
- Phase 7 (2026-09-02) implemented EarningsReleaseProcessor and closed with status `EARNINGS_RELEASE_PROCESSOR_CLOSED`. The LTTS Q1 FY27 press/investor release (6965ca6d) was reclassified from QUARTERLY_REPORT to EARNINGS_RELEASE; LTTS company onboarding added to `companies/ltts/company_registry.json`. New `_RELEASE_SIGNALS` added to the identifier; release figures carry `authority=EARNINGS_RELEASE` and do not overwrite AUDITED financial truth; CEO/CFO quote blocks enter common evidence as management claims with source period / target period / qualifier preservation. Document-scoped storage under `companies/<company>/<fy>/earnings_releases/<hash>/`. `EarningsReleaseProcessor` registered after direct production proof. Router now declares `EARNINGS_RELEASE → AVAILABLE`. Misleading filename proof confirmed content-based identity. 13 focused release tests + full suite (1877 passed, 9 pre-existing failures). ENG-070 partially addressed (LTTS onboarded); systematic onboarding tooling remains deferred. ENG-071 not encountered; remains open.
- Phase 8 (2026-09-02) established the ExchangeDisclosure architecture contract with status `BLOCKED_REAL_EXCHANGE_DISCLOSURE_MISSING`. A full repository inventory confirmed no genuine event-centric exchange disclosure existed. Despite the stop condition, the complete architecture was implemented: `DisclosureEventType` (17 values), `DisclosureEventStatus` (15 values), `_DISC_STATUS_PATTERNS` (13 ordered patterns enforcing PROPOSED ≠ APPROVED ≠ COMPLETED), `_DISCLOSURE_SIGNALS` in the identifier (11 patterns), `ExchangeDisclosureProcessor` class in `document_processor.py` (NOT registered). Key semantic contracts: "completed" (past tense only) ≠ "expected to complete"; board approval ≠ commercial closure; commissioned ≠ operational; vague amount never invented; unnamed counterparty stays None. 49 adversarial tests added.
- Phase 8.1 (2026-09-03) completed the production gate: a real SE Intimation PDF (dual-CXO resignation, Reg 30 SEBI filing) was supplied. All gates passed: Gate A (blind `identify_document()` → `EXCHANGE_DISCLOSURE`; required three fixes: stronger signals, payload fallback, EXCHANGE_FILING demote), Gate B (manual event-semantic verification — 2 MANAGEMENT_CHANGE events, filing date ≠ event date, no counterparty, no amount, no false status), Gate C (direct `processor.process()` → SUCCESS with 2 events; fixed SIGNED false positive from digital signature blocks and added "Month DD, YYYY" date format to `_RE_DISC_DATE`), Gate D (misleading filename → identical classification). Pipeline proof: `execute=False` → `ROUTABLE/AVAILABLE`; `execute=True` → `EXECUTED/SUCCESS`. Cross-source regression: 266 passed. **`ExchangeDisclosureProcessor` registered. `EXCHANGE_DISCLOSURE → AVAILABLE` in router. ENG-065 CLOSED — all 6 source-type processors production-validated.**
- Not built yet (deferred): exchange-disclosure processor registration (blocked), operator review UI for REVIEW_REQUIRED manifests, duplicate/version management, systematic company onboarding CLI (ENG-070), downstream consumption of EARNINGS_RELEASE / EXCHANGE_DISCLOSURE evidence by Management Commitments / Progression.

## 5. Customer-Facing Frontend Surface

- `apps/ask-intrinsiciq/` is the canonical standalone Next.js frontend for the customer-facing IntrinsicIQ experience named Ask IntrinsicIQ.
- Prometheus remains the internal intelligence engine; customer-facing UI copy must prefer `IntrinsicIQ` / `Ask IntrinsicIQ` and must not surface raw Prometheus terminology, raw JSON artifacts, CIM/PCIM labels, or pipeline-internal language.
- Ask IntrinsicIQ v0 is intentionally desktop-first and build-safe. It must not require authentication, API calls, or database access, and it must not expose raw Prometheus terminology, artifact names, JSON filenames, diagnostics, or pipeline wording in the customer-facing UI. The canonical frontend runtime path is now a server-only loader under `apps/ask-intrinsiciq/src/lib/ask-intrinsiciq/` that reads only the governed Ask IntrinsicIQ output directory, validates the runtime payload, and hands typed UI-facing data to React components.
- The canonical backend producer for Ask IntrinsicIQ public outputs is `intelligence/ask_intrinsiciq/`. Its deterministic pass now writes `business_journey.json`, `products_services.json`, `answer_cards.json`, `financial_visual_summaries.json`, `uncertainty_map.json`, a refreshed public `company_research_view.json`, plus `ask_intrinsiciq_manifest.json` and `ask_intrinsiciq_validation_report.json`.
- The Ask IntrinsicIQ business-journey generator is a deterministic company-memory reader only. It may use multi-year company memory, CIM/PCIM business-understanding outputs, management commentary already present in company memory, and committee synthesis as a late high-level fallback, but it must not read raw annual-report text directly and must not present management ambition as completed execution.
- The Ask IntrinsicIQ answer-card generator is a deterministic company-memory reader only. It owns the canonical curated question catalog for the five v0 categories (`understand-the-business`, `financials`, `management`, `investor-panel`, and `risks-and-diligence`), must generate one public answer card for every curated question, must preserve `supported` / `partially_supported` / `not_supported` / `unavailable` distinctions without inventing missing evidence, must keep exactly three valid next-question links per answer, and must not expose internal system language, buy/sell/hold advice, or valuation conclusions.
- Ask IntrinsicIQ answer cards now also own the explicit question-to-context routing policy for optional answer sections. `answer_cards.json` is the canonical boundary for whether an answer may show business-journey context, products/services context, or financial visuals, and the frontend loader must not inject company-level fallback journey or visual sections when the answer card does not explicitly request them. The current governed public routing field is `business_journey_mode` (`none` or `full`) plus existing explicit product and visual references.
- Ask IntrinsicIQ answer cards now also support additive public answer-structure fields when the evidence supports them: `customer_roles`, `revenue_flow`, and `structured_sections`. `customer_roles` is the canonical Who pays / Who integrates or partners / Who uses / International customers object and must preserve uncertainty rather than mechanically copying one mixed audience list into every bucket. `revenue_flow` is the canonical compact monetization flow object for revenue-model answers and must stay UI-facing, investor-readable, and free of internal artifact wording. `structured_sections` is the canonical titled-section surface for curated doctrine-style answers such as Buffett.
- Ask IntrinsicIQ answer-card writing now includes a deterministic final truth-reconciliation boundary before public save. `finalize_answer_against_reconciled_truth(...)` is the canonical cleanup step: reconciled financial truth and governed investor-financial modules outrank stale investor-lens or committee missing-data wording, public answers may not claim CFO/capex/FCF/owner-earnings absence when those values are already visible in governed truth, and surviving limitations must be rewritten into precision, comparability, basis, concentration, or history language instead of broad false-missing language.
- Ask IntrinsicIQ answer cards also now include deterministic public-quality guards for contradiction and repetition. Public answer composition must reject or rewrite avoidable repetition across `simple_answer`, `key_points`, `evidence_summary`, and `uncertainty_note`, and validator coverage must flag stale non-customer-facing wording or missing-data contradictions before the artifact is treated as clean.
- Ask IntrinsicIQ answer cards now also carry an explicit `progression` block for business-memory questions. Public answers should surface what changed, why it changed, current state, turning points, unresolved items, and conviction impact so the product reads as company memory over time rather than a static screener snapshot.
- The Ask IntrinsicIQ financial-visual generator is a deterministic company-memory reader only. It may read financial truth, investor-financial modules, and validated financial-memory artifacts already written by upstream stages, but it must not add an LLM call, must not recompute financial truth independently, must not zero-fill missing historical values, must keep candidate visuals compact (`max 3 series`, `max 8 points per series`), and must preserve `reported` / `derived` / `partial` / `missing` / `unreliable` point status plus `direct` / `derived` / `partial` / `missing` / `unreliable` visual evidence status for downstream UI rendering.
- Ask IntrinsicIQ financial visuals may now carry an optional point-level `semantic_label` for bridge-style or repeated-period visuals so the frontend can render compact chips without ambiguous unlabeled values. This is additive to the existing point contract and does not authorize the frontend to infer missing labels.
- Ask IntrinsicIQ products/services output remains deterministic and company-memory only. The governed public grouping ceiling is now 5 sections, but public output must not render empty groups and should stay within 8 offering items plus 4 capability items overall. Public items must separate sellable offerings from capabilities when the evidence supports it, may now carry additive UI-safe `group`, `customer_types`, `revenue_model`, and `source_periods` fields, and the frontend should present explanation, customer context, role in business, and evidence status rather than chip-only tags.
- Ask IntrinsicIQ business journey remains deterministic and company-memory only, but the current v0 public journey ceiling is now 3 historical stages plus one current-state view. The backend contract must separate `historical_stages`, `current_state`, and `stated_direction`, while preserving the existing `stages`/`current_direction` compatibility surface for the current frontend view model. Public stages should represent material business phases rather than one stage per year, must not present management ambition as achieved fact, and must run through deterministic quality checks for duplicate phases, broken sentences, vague filler language, and repeated-title drift.
- The Ask IntrinsicIQ uncertainty mapper is a deterministic company-memory reader only. It must convert meaningful uncertainty, missing evidence, precision limits, unreliable data, and material unresolved questions into investor-readable public outputs without exposing internal artifact labels, validation/debug wording, or raw panel diagnostics. It must explain what is missing, why it matters, which curated questions are affected, and what the next diligence question should be, while avoiding broad repetitive warnings on answers that are otherwise well supported. Question-level uncertainty-note routing is now a governed deterministic ranking policy rather than a pure global-severity pick.
- The v0 canonical route set is:
  - `/` for company search landing.
  - `/company/[companySlug]` for the curated company question navigator, generated only for companies with a valid `company_memory/ask_intrinsiciq/` output directory.
  - `/company/[companySlug]/question/[questionId]` for focused answer screens, statically generated from the canonical question catalog already present on `company_research_view.json`.
- Canonical frontend typed UI content for v0 lives under `apps/ask-intrinsiciq/src/data/`, with domain types under `apps/ask-intrinsiciq/src/types/` and route/helpers under `apps/ask-intrinsiciq/src/lib/`. Company-specific intelligence shown in routes must come from canonical Ask IntrinsicIQ backend outputs through the server-only loader boundary rather than from component-local authored copy.
- `apps/ask-intrinsiciq/ASK_INTRINSICIQ_VIEW_MODEL_CONTRACT.md` is the canonical implementation-ready frontend data contract for Prometheus-to-Ask IntrinsicIQ integration. It defines the stable company-agnostic UI view model that future backend/read-model producers must satisfy before raw Prometheus artifacts can be treated as frontend-ready.
- `intelligence/ask_intrinsiciq/` must keep its backend contracts aligned to `apps/ask-intrinsiciq/ASK_INTRINSICIQ_VIEW_MODEL_CONTRACT.md`. Public Ask IntrinsicIQ outputs must not expose internal system terms such as PCIM/CIM, raw filenames, JSON wording, artifact labels, prompt/LLM language, evidence IDs, or pipeline diagnostics.
- The canonical v0 interaction contract is governed by `apps/ask-intrinsiciq/PRODUCT_UI_CONTRACT.md`: one category open at a time, no more than five visible questions, one subtle recommended-next indicator, progressive evidence disclosure collapsed by default, exactly three next-question options on answer screens, and no investor-lens switcher, dashboard, charting, custom-question input, or research-cockpit surface.
- The refined v0 navigator contract also allows a subtle active-category progress cue and requires every curated question to resolve to a valid route. The production/default path must render canonical backend answers only; a synthetic development fixture may appear only when canonical output is absent and must remain visibly marked as a development-only fallback.
- The Ask IntrinsicIQ answer contract now distinguishes UI-facing answer support states through a typed frontend boundary: `supported`, `partially_supported`, `not_supported`, and `unavailable`. Customer-facing screens may show those states through subtle wording only; they must not expose raw artifact names, source filenames, validation-report details, or internal pipeline language when evidence is thin or unavailable.

## 6. Canonical Frontend Folder Contract

- `apps/ask-intrinsiciq/app/`: route entrypoints only.
- `apps/ask-intrinsiciq/src/components/`: reusable UI components grouped by screen/domain. The canonical v0 component layer includes `brand-mark`, `company-search`, `company-header`, `question-category`, `question-chip`, `question-navigator`, `answer-view`, `evidence-disclosure`, `next-questions`, `business-journey-timeline`, `products-services-section`, `financial-visual-renderer`, `customer-role-breakdown`, `revenue-flow`, and `structured-answer-sections`.
- `apps/ask-intrinsiciq/src/data/`: typed static UI content and development-only fixtures. Production/default company intelligence must not be authored here.
- `apps/ask-intrinsiciq/src/types/`: frontend research/view-model types. The canonical `ResearchAnswer` contract now carries a UI-facing `answerStatus` plus concise-answer / why-it-matters aliases so sourced, partially sourced, not-supported, and placeholder states can render through one calm answer screen without component-level source branching.
- `apps/ask-intrinsiciq/src/types/`: frontend research/view-model types. The canonical frozen v0 view-model contract is now split across `company-research-view.ts`, `research-question.ts`, `research-answer.ts`, `business-journey.ts`, `products-services.ts`, `financial-visuals.ts`, `evidence.ts`, and `index.ts`. The older `research.ts` file remains the current renderer-compatibility layer until the app migrates fully onto the frozen contract.
- `apps/ask-intrinsiciq/src/lib/`: local route helpers and frontend-only utilities. `src/lib/ask-intrinsiciq/` is the canonical server-only frontend loader boundary for reading only governed Ask IntrinsicIQ backend outputs (`company_research_view.json`, `business_journey.json`, `products_services.json`, `answer_cards.json`, `financial_visual_summaries.json`, `uncertainty_map.json`, `ask_intrinsiciq_manifest.json`, and `ask_intrinsiciq_validation_report.json`), validating runtime safety, and mapping those files into typed UI-facing view models without leaking internal filenames or diagnostic fields into UI components.
- `intelligence/ask_intrinsiciq/`: canonical backend Ask IntrinsicIQ output layer. It owns deterministic source loading, path resolution, public-text sanitation, business-journey generation, products/services generation, answer-card generation, financial-visual generation, manifest/report writing, and public `company_research_view.json` generation under `companies/<company>/company_memory/ask_intrinsiciq/`.
- `intelligence/ask_intrinsiciq/products_services.py`: canonical deterministic products-and-services mapper for Ask IntrinsicIQ. It may read only existing company-memory intelligence, must not parse raw annual-report text directly, must not add new LLM calls, must preserve uncertainty when economic contribution is unknown, and must keep internal provenance in diagnostics-only structures rather than public output payloads.
- `intelligence/ask_intrinsiciq/answer_cards.py`: canonical deterministic answer-card generator for Ask IntrinsicIQ. It may read existing company-memory, financial-truth, investor-financial-module, investor-panel, committee-synthesis, business-journey, and products/services outputs only; it must not add new LLM calls, must not modify upstream financial or investor-panel logic, must keep unsupported questions as conservative public answers rather than placeholders or omissions, and must keep the backend question catalog aligned with the customer-facing Ask IntrinsicIQ route catalog.
- `intelligence/ask_intrinsiciq/financial_visuals.py`: canonical deterministic financial-visual summary generator for Ask IntrinsicIQ. It must build only UI-facing chart-ready meaning from existing financial truth and investor-financial-module evidence, write `financial_visual_summaries.json`, keep public copy free of internal file/artifact labels, and expose only compact visual semantics (`id`, `title`, `subtitle`, `visual_type`, `unit`, `series`, `interpretation`, `precision_note`, `evidence_status`, `recommended_display`, `generated_at`) plus conservative coverage counts.
- `intelligence/ask_intrinsiciq/uncertainty_mapper.py`: canonical deterministic uncertainty and missing-evidence mapper for Ask IntrinsicIQ. It must write `uncertainty_map.json`, deduplicate materially similar gaps such as capex-split and owner-earnings-precision limits into one clear public item, classify uncertainty as `missing`, `partial`, `precision_limited`, `unreliable`, or `contradictory`, assign conservative severity, map each item to affected curated question IDs, and provide one investor-readable suggested follow-up question per item.
- Frontend tests may live adjacent to components when they validate route links or screen interaction contracts such as the question-category accordion.

## 10. Next Milestone

### Investor-intelligence semantic boundary

- Every upstream company-memory candidate must carry actor/ownership, statement or stream classification, investor relevance, materiality, period basis, eligibility, evidence confidence, and semantic status before promotion.
- Management commitments accept company-owned commitment speech acts only. Government targets, industry forecasts, historical facts, existing capabilities, CSR activity, and unbounded aspirations remain quarantined.
- Projects, capacity, commentary, risks, and capital allocation must enforce their own economic boundaries. Structural validity cannot substitute for investor relevance.
- Management Quality is an upstream-only synthesis. It consumes commitments, projects, capacity, risks, commentary, capital allocation, and governed financial evidence; it must not consume Investor Panel or Committee conclusions.
- A synthesized artifact must not predate an upstream dependency it claims to consume. Stale lineage is a hard failure.
- Investor Panel, Committee, and Ask IntrinsicIQ may be regenerated only after all seven upstream semantic gates pass and have been manually audited.

### Production financial-output reuse and blocking contract

- The `production` profile may reuse an existing year-level fundamentals set only when every governed financial artifact is present, each artifact embeds the active company and year, validation and reconciliation are non-failing, and the year-level financial audit is `pass` or `warning` with no hard failures. Reuse is same-period only; values from another reporting year must never be substituted.
- Primary balance-sheet extraction may restore a generic `TOTAL` to `Total Assets` only at the explicit assets-to-`EQUITY AND LIABILITIES` boundary. Ordinary subtotals remain generic and unmapped.
- A failing year-level `financial_audit_report.json` is a blocking `financials` stage result. Production must stop before financial memory, panel, or customer-facing output rather than treating a completed but failed audit as success.
- Current shares outstanding may use an explicit end-of-period share-capital-note row, but opening/beginning share counts are not current-period substitutes. Monetary fundamentals accept only monetary-typed extracted values, and net worth may be transparently derived as sourced equity share capital plus sourced reserves with formula/provenance retained.
- Primary balance-sheet discovery is both heading-aware and structure-aware. A titleless/split-page statement may be recognized from period columns, table headers, assets, liabilities, and equity structure even when the statement title is on an adjacent page or trailing accounting-policy text shares the chunk. A usable balance sheet requires assets, equity/net worth, and liabilities; debt itself is optional. Total liabilities may be transparently derived as `total_assets - net_worth` when both inputs are sourced and the result is non-negative, but missing debt must remain missing rather than being fabricated.
- Structural statement classification must be mutually guarded: an explicit or structural operating-cash-flow signature takes precedence over the balance-sheet fallback because cash-flow working-capital and financing rows naturally mention assets, liabilities, and equity. Titleless cash-flow pages may be recognized from period columns, table headers, and operating-cash-flow rows.
- LLM input-pack budgets apply to the final serialized pack, including size metadata and truncation counters. Pack size metadata must be measured to a serialization fixed point. Business-intelligence compaction reserves bounded serialization headroom so metadata updates cannot push an otherwise compacted pack over its declared stage budget.

Regenerate stale committee synthesis artifacts against the new financial committee contract, then continue tightening investor-panel prompt compaction for real company PCIMs while keeping doctrine-declared financial coverage and the no-new-ratio rule intact.
